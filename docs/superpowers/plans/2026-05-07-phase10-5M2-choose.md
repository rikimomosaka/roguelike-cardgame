# Phase 10.5.M2-Choose: UI Card Selection Modal Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 4 actions (`discard` / `exhaustCard` / `upgrade` / `recoverFromDiscard`) で `Select="choose"` 指定時に、プレイヤーがどのカードを対象にするか UI で選択できるようにする (現状は random fallback)。

**Architecture:** `BattleState.PendingCardPlay` (in-memory only) で pause/resume パターン。`BattleEngine.PlayCard` で choose 必要 effect を検出すると pending を emit して early return、Client で modal 表示、`POST /battle/resolve-card-choice` endpoint で resume。複合 choose は順次 pause (semantically 正しい)、N >= candidates なら auto-skip (random/all 経路に流す)。Pile == hand なら手札 UI、それ以外 (draw/discard) はリスト UI。

**Action lockdown (重要):** PendingCardPlay が non-null の間、プレイヤーの他アクションは全て blocked (別カードプレイ・ポーション使用・ターン終了・ターゲット変更等)。Server 側は全 player-action endpoint (play-card / end-turn / use-potion / set-target / finalize) で 409 を返し、Client 側は modal 表示中は他 UI control を全 disable して exclusively choose modal を出す (modal 自体は closeOnEsc=false で外し不可)。

**Tech Stack:** C# .NET 10、xUnit、ImmutableArray ベースの record state、ASP.NET Core 10、React 19 + Vite。

**Spec reference:** [docs/superpowers/specs/2026-05-07-phase10-5M2-choose-design.md](docs/superpowers/specs/2026-05-07-phase10-5M2-choose-design.md)

---

## File Structure

**Create:**
- `src/Core/Battle/State/PendingCardPlay.cs` — `PendingCardPlay` + `PendingChoice` record 定義
- `src/Server/Dtos/PendingCardPlayDto.cs` — DTO
- `src/Client/src/screens/battleScreen/CardChoiceModal.tsx` — modal component
- `src/Client/src/screens/battleScreen/CardChoiceModal.css` — modal styles
- `tests/Core.Tests/Battle/Engine/BattleEngineChooseTests.cs` — pause / resume tests
- `tests/Core.Tests/Battle/Engine/EffectApplierChooseTests.cs` — per-action choose tests
- `tests/Server.Tests/Controllers/BattleControllerResolveChoiceTests.cs` — endpoint tests
- `tests/Client/src/screens/battleScreen/CardChoiceModal.test.tsx` — client tests

**Modify:**
- `src/Core/Battle/State/BattleState.cs` — `PendingCardPlay` field 追加
- `src/Core/Battle/Engine/BattleEngine.PlayCard.cs` — per-effect state machine refactor (`ApplyEffectsFrom` 抽出 + pending emit + `summonSucceededBefore` snapshot)
- `src/Core/Battle/Engine/BattleEngine.cs` — `ResolveCardChoice` 公開メソッド追加
- `src/Core/Battle/Engine/EffectApplier.cs` — 4 actions の choose 検出 + 適用ロジック分離
- `src/Server/Controllers/BattleController.cs` — `POST /battle/resolve-card-choice` endpoint
- `src/Server/Services/BattleStateDtoMapper.cs` — `PendingCardPlay` field マッピング
- `src/Client/src/api/types.ts` — `BattleStateDto.pendingCardPlay` 型追加
- `src/Client/src/api/battle.ts` (or 新規) — `resolveCardChoice` API client
- `src/Client/src/screens/BattleScreen.tsx` — `CardChoiceModal` 統合 + Hand 操作 disabled

**Test:** 上記新規 test ファイル + 既存 PlayCard / EffectApplier tests に regression なし

---

## Task 1: PendingCardPlay / PendingChoice record 定義 + BattleState field 追加

**Files:**
- Create: `src/Core/Battle/State/PendingCardPlay.cs`
- Modify: `src/Core/Battle/State/BattleState.cs` (末尾 default null field 追加)

- [ ] **Step 1.1: PendingCardPlay.cs を新規作成**

```csharp
using System.Collections.Immutable;

namespace RoguelikeCardGame.Core.Battle.State;

/// <summary>
/// Phase 10.5.M2-Choose: PlayCard 中で choose effect に到達した時の pause 状態。
/// Resolve されるまで PlayCard は呼べない (validate で reject)。
/// BattleState は in-memory のみ (Server BattleSession.State) なので save schema 影響なし。
/// </summary>
public sealed record PendingCardPlay(
    string CardInstanceId,
    int EffectIndex,
    bool SummonSucceededBefore,
    PendingChoice Choice);

/// <summary>
/// Phase 10.5.M2-Choose: choose effect の選択候補と要件。
/// </summary>
public sealed record PendingChoice(
    string Action,
    string Pile,
    int Count,
    ImmutableArray<string> CandidateInstanceIds);
```

- [ ] **Step 1.2: BattleState に PendingCardPlay field 追加**

`src/Core/Battle/State/BattleState.cs` の record 末尾 (`int DrawPerTurn = 5,` の後) に追加:

```csharp
public sealed record BattleState(
    int Turn,
    BattlePhase Phase,
    BattleOutcome Outcome,
    ImmutableArray<CombatActor> Allies,
    ImmutableArray<CombatActor> Enemies,
    int? TargetAllyIndex,
    int? TargetEnemyIndex,
    int Energy,
    int EnergyMax,
    ImmutableArray<BattleCardInstance> DrawPile,
    ImmutableArray<BattleCardInstance> Hand,
    ImmutableArray<BattleCardInstance> DiscardPile,
    ImmutableArray<BattleCardInstance> ExhaustPile,
    ImmutableArray<BattleCardInstance> SummonHeld,
    ImmutableArray<BattleCardInstance> PowerCards,
    int ComboCount,
    int? LastPlayedOrigCost,
    bool NextCardComboFreePass,
    ImmutableArray<string> OwnedRelicIds,
    ImmutableArray<string> Potions,
    string EncounterId,
    bool WildUsedInCurrentCombo = false,
    int DrawPerTurn = 5,
    /// <summary>Phase 10.5.M2-Choose: choose 中の保留状態。null=非保留。</summary>
    PendingCardPlay? PendingCardPlay = null);
```

(default null で既存 `new BattleState(...)` 全箇所が無変更で動く。)

- [ ] **Step 1.3: 全 Core テスト緑確認 (regression なし)**

```bash
dotnet test tests/Core.Tests/Core.Tests.csproj
```
Expected: 1251 PASS (既存と同じ件数、新コード未追加なので)。

- [ ] **Step 1.4: Commit + push**

```bash
git add src/Core/Battle/State/PendingCardPlay.cs src/Core/Battle/State/BattleState.cs
git commit -m "$(cat <<'EOF'
feat(battle): PendingCardPlay / PendingChoice records + BattleState field (Phase 10.5.M2-Choose T1)

choose effect で PlayCard を pause/resume するための保留状態。
- PendingCardPlay: CardInstanceId / EffectIndex / SummonSucceededBefore / Choice
- PendingChoice: Action / Pile / Count / CandidateInstanceIds[]
- BattleState 末尾に default null field 追加 (既存 instantiation 互換)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push origin master
```

---

## Task 2: PlayCard を per-effect state machine に refactor (behavior 変更なし)

**Files:**
- Modify: `src/Core/Battle/Engine/BattleEngine.PlayCard.cs`

**Goal:** 既存 `foreach (var eff in effects)` ループを `ApplyEffectsFrom(state, ..., startIndex)` private static method に抽出。**この時点では behavior 変更ゼロ** (pending emit はしない、既存の random fallback まま)、既存テスト全て通るはず。後の T3 で choose 検出を追加する基盤。

- [ ] **Step 2.1: ApplyEffectsFrom helper を抽出**

`src/Core/Battle/Engine/BattleEngine.PlayCard.cs` の effects loop を private helper に切り出す。

ファイル末尾 (`}` の前) に追加:

```csharp
    /// <summary>
    /// Phase 10.5.M2-Choose: effect ループを resume 可能な形に切り出した内部 helper。
    /// startIndex から effects[i] を順次適用、summonSucceeded は in-out で受け渡す。
    /// PlayCard 初回呼出時は startIndex=0 / summonSucceededBefore=false から、
    /// ResolveCardChoice からの resume では pending.EffectIndex+1 / pending.SummonSucceededBefore から呼ぶ。
    /// </summary>
    /// <returns>(更新後 state, 発火 events, 最終 summonSucceeded フラグ, 次に処理する effect index = effects.Count なら全完了)</returns>
    private static (BattleState state, List<BattleEvent> events, bool summonSucceeded, int nextIndex) ApplyEffectsFrom(
        BattleState state, CombatActor caster, BattleCardInstance card,
        IReadOnlyList<CardEffect> effects, int startIndex, int newCombo,
        bool summonSucceededIn, ref int order,
        IRng rng, DataCatalog catalog)
    {
        var s = state;
        var events = new List<BattleEvent>();
        bool summonSucceeded = summonSucceededIn;
        for (int i = startIndex; i < effects.Count; i++)
        {
            var eff = effects[i];
            // 10.5.E: Trigger 指定 effect は即時実行ではなく、PowerTriggerProcessor 経由
            if (!string.IsNullOrEmpty(eff.Trigger)) continue;
            // 10.2.C: per-effect comboMin filter
            if (eff.ComboMin is { } min && newCombo < min) continue;

            int beforeAlliesLength = s.Allies.Length;
            var (afterEffect, evs) = EffectApplier.Apply(s, caster, eff, rng, catalog);
            s = afterEffect;
            foreach (var ev in evs)
            {
                events.Add(ev with { Order = order });
                order++;
            }
            caster = s.Allies[0];

            // 10.2.D: summon 成功検出
            if (eff.Action == "summon" && s.Allies.Length > beforeAlliesLength)
                summonSucceeded = true;
        }
        return (s, events, summonSucceeded, effects.Count);
    }
```

- [ ] **Step 2.2: PlayCard 本体で ApplyEffectsFrom を呼び出すよう書き換え**

既存の `foreach (var eff in effects)` ブロック (行 101〜125) を以下に置換:

```csharp
        var (afterEffects, effectEvents, summonSucceeded, _) = ApplyEffectsFrom(
            s, caster, card, effects, startIndex: 0, newCombo, summonSucceededIn: false,
            ref order, rng, catalog);
        s = afterEffects;
        events.AddRange(effectEvents);
        caster = s.Allies[0];
```

(`bool summonSucceeded = false;` の宣言は削除。`order` は ref で渡しているので caller の値が更新される。)

- [ ] **Step 2.3: 全 Core テスト緑確認 (behavior 変更なし)**

```bash
dotnet test tests/Core.Tests/Core.Tests.csproj
```
Expected: 1251 PASS。既存テストが 1 件でも落ちたら refactor バグ → revert。

- [ ] **Step 2.4: Commit + push**

```bash
git add src/Core/Battle/Engine/BattleEngine.PlayCard.cs
git commit -m "$(cat <<'EOF'
refactor(battle): extract effect loop into ApplyEffectsFrom helper (Phase 10.5.M2-Choose T2)

PlayCard の effects 適用ループを private static ApplyEffectsFrom に切り出し。
behavior 変更なし。後の T3-T4 で pause/resume 基盤として使用する準備。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push origin master
```

---

## Task 3: 4 actions で NeedsPlayerChoice 判定 + pending emit (PlayCard pause)

**Files:**
- Modify: `src/Core/Battle/Engine/EffectApplier.cs`
- Modify: `src/Core/Battle/Engine/BattleEngine.PlayCard.cs`
- Test: `tests/Core.Tests/Battle/Engine/BattleEngineChooseTests.cs` (新規)

**Goal:** ApplyEffectsFrom 内で choose effect を検出したら PendingCardPlay を返して early return。EffectApplier に `NeedsPlayerChoice` + `BuildPendingChoice` 公開 helper を追加。

- [ ] **Step 3.1: EffectApplier に choose detection helper 追加**

`src/Core/Battle/Engine/EffectApplier.cs` ファイル末尾 (class brace 直前) に追加:

```csharp
    // ---- Phase 10.5.M2-Choose: choose 検出 + 候補構築 ----

    /// <summary>
    /// Choose effect で実際にプレイヤー選択 modal を出すべきかを判定。
    /// candidates ≤ Amount の場合は auto-skip (existing random/all path で全自動処理)。
    /// 4 action (discard / exhaustCard / upgrade / recoverFromDiscard) のみ対応。
    /// </summary>
    public static bool NeedsPlayerChoice(BattleState state, CardEffect effect, DataCatalog catalog)
    {
        if (effect.Select != "choose") return false;
        if (!IsChooseSupportedAction(effect.Action)) return false;
        var candidates = GetChooseCandidates(state, effect, catalog);
        return candidates.Length > effect.Amount;
    }

    /// <summary>Choose 対応 4 action 一覧。</summary>
    private static bool IsChooseSupportedAction(string action)
        => action is "discard" or "exhaustCard" or "upgrade" or "recoverFromDiscard";

    /// <summary>
    /// 各 action の choose 候補 (BattleCardInstance.InstanceId 一覧) を返す。
    /// upgrade のみ「強化可能 (IsUpgradable && !IsUpgraded)」フィルタ適用。
    /// </summary>
    public static System.Collections.Immutable.ImmutableArray<string> GetChooseCandidates(
        BattleState state, CardEffect effect, DataCatalog catalog)
    {
        var pile = ResolveChoosePile(state, effect);
        if (effect.Action == "upgrade")
        {
            var builder = System.Collections.Immutable.ImmutableArray.CreateBuilder<string>();
            foreach (var c in pile)
            {
                if (c.IsUpgraded) continue;
                if (!catalog.TryGetCard(c.CardDefinitionId, out var def)) continue;
                if (!def.IsUpgradable) continue;
                builder.Add(c.InstanceId);
            }
            return builder.ToImmutable();
        }
        // discard / exhaustCard / recoverFromDiscard は pile 全カード
        return pile.Select(c => c.InstanceId).ToImmutableArray();
    }

    private static System.Collections.Immutable.ImmutableArray<BattleCardInstance> ResolveChoosePile(
        BattleState state, CardEffect effect)
    {
        // discard と recoverFromDiscard は pile 固定
        if (effect.Action == "discard") return state.Hand;
        if (effect.Action == "recoverFromDiscard") return state.DiscardPile;
        // exhaustCard / upgrade は effect.Pile 指定
        return (effect.Pile ?? "hand") switch
        {
            "hand" => state.Hand,
            "draw" => state.DrawPile,
            "discard" => state.DiscardPile,
            _ => System.Collections.Immutable.ImmutableArray<BattleCardInstance>.Empty,
        };
    }

    /// <summary>Pause 時の PendingChoice 情報を構築する (Pile を文字列で正規化)。</summary>
    public static PendingChoice BuildPendingChoice(BattleState state, CardEffect effect, DataCatalog catalog)
    {
        string pile = effect.Action switch
        {
            "discard" => "hand",
            "recoverFromDiscard" => "discard",
            _ => effect.Pile ?? "hand",
        };
        return new PendingChoice(
            Action: effect.Action,
            Pile: pile,
            Count: effect.Amount,
            CandidateInstanceIds: GetChooseCandidates(state, effect, catalog));
    }
```

(import: `using RoguelikeCardGame.Core.Battle.State;` は既存。`using System.Collections.Immutable;` も既存のはず。)

- [ ] **Step 3.2: ApplyEffectsFrom で pause emit**

`src/Core/Battle/Engine/BattleEngine.PlayCard.cs` の `ApplyEffectsFrom` 内、 `EffectApplier.Apply(...)` 呼び出しの **直前** に choose 判定を追加:

```csharp
        for (int i = startIndex; i < effects.Count; i++)
        {
            var eff = effects[i];
            if (!string.IsNullOrEmpty(eff.Trigger)) continue;
            if (eff.ComboMin is { } min && newCombo < min) continue;

            // Phase 10.5.M2-Choose: choose effect で pause が必要なら early return
            if (EffectApplier.NeedsPlayerChoice(s, eff, catalog))
            {
                var pending = new PendingCardPlay(
                    CardInstanceId: card.InstanceId,
                    EffectIndex: i,
                    SummonSucceededBefore: summonSucceeded,
                    Choice: EffectApplier.BuildPendingChoice(s, eff, catalog));
                var paused = s with { PendingCardPlay = pending };
                return (paused, events, summonSucceeded, i);  // i を nextIndex として返す (== pause 位置)
            }

            int beforeAlliesLength = s.Allies.Length;
            var (afterEffect, evs) = EffectApplier.Apply(s, caster, eff, rng, catalog);
            // ... 既存
        }
```

- [ ] **Step 3.3: PlayCard 本体で pending を check して early return**

`PlayCard` の `ApplyEffectsFrom` 呼び出し直後に追加:

```csharp
        var (afterEffects, effectEvents, summonSucceeded, nextIndex) = ApplyEffectsFrom(
            s, caster, card, effects, startIndex: 0, newCombo, summonSucceededIn: false,
            ref order, rng, catalog);
        s = afterEffects;
        events.AddRange(effectEvents);

        // Phase 10.5.M2-Choose: pause 中は OnPlayCard / カード移動 / Power トリガを発火せず即返却
        if (s.PendingCardPlay is not null)
            return (s, events);

        caster = s.Allies[0];
```

- [ ] **Step 3.4: PlayCard の pending validation を追加**

`PlayCard` メソッド冒頭の validation ブロック (`if (state.Phase != BattlePhase.PlayerInput)` の後) に追加:

```csharp
        // Phase 10.5.M2-Choose: 既存 pending を resolve しないと PlayCard 不可
        if (state.PendingCardPlay is not null)
            throw new InvalidOperationException(
                "Cannot play card while PendingCardPlay is set; resolve via ResolveCardChoice first");
```

- [ ] **Step 3.5: 失敗テスト追加 (BattleEngineChooseTests)**

新規 `tests/Core.Tests/Battle/Engine/BattleEngineChooseTests.cs`:

```csharp
using System.Collections.Immutable;
using System.Linq;
using RoguelikeCardGame.Core.Battle.Engine;
using RoguelikeCardGame.Core.Battle.State;
using RoguelikeCardGame.Core.Cards;
using RoguelikeCardGame.Core.Random;
using RoguelikeCardGame.Core.Tests.Battle.Fixtures;
using Xunit;

namespace RoguelikeCardGame.Core.Tests.Battle.Engine;

public class BattleEngineChooseTests
{
    private static IRng Rng() => new FakeRng(new int[20], new double[0]);

    [Fact]
    public void PlayCard_ExhaustCardChoose_HandHas3Cards_PausesWithPending()
    {
        // Arrange: hand に 3 枚、exhaustCard choose count=1 のカードをプレイ
        var fakeChooseCard = ChooseTestFixtures.MakeExhaustCardChooseCard("exhaust_choose", amount: 1);
        var state = ChooseTestFixtures.StateWithHandAndCard(
            hand: new[] { "card_a", "card_b", "card_c", fakeChooseCard.id },
            chooseCardId: fakeChooseCard.id);
        var catalog = ChooseTestFixtures.CatalogWith(fakeChooseCard.def);

        // Act
        var (next, events) = BattleEngine.PlayCard(state, handIndex: 3, null, null, Rng(), catalog);

        // Assert: pending 設定済 + Choice 内容
        Assert.NotNull(next.PendingCardPlay);
        var pending = next.PendingCardPlay!;
        Assert.Equal(fakeChooseCard.id, pending.CardInstanceId);
        Assert.Equal(0, pending.EffectIndex);
        Assert.Equal("exhaustCard", pending.Choice.Action);
        Assert.Equal("hand", pending.Choice.Pile);
        Assert.Equal(1, pending.Choice.Count);
        // 候補は手札全部 (4 枚: choose card 自身も含む — engine は実装上のシンプルさで除外しない)
        Assert.Equal(4, pending.Choice.CandidateInstanceIds.Length);
    }

    [Fact]
    public void PlayCard_ExhaustCardChoose_HandHas1Card_AutoSkipsToRandom()
    {
        // Arrange: 手札 1 枚 (choose card 自身のみ)、exhaustCard choose count=2 → candidates ≤ Amount で auto-skip
        var fakeChooseCard = ChooseTestFixtures.MakeExhaustCardChooseCard("exhaust_choose", amount: 2);
        var state = ChooseTestFixtures.StateWithHandAndCard(
            hand: new[] { fakeChooseCard.id },
            chooseCardId: fakeChooseCard.id);
        var catalog = ChooseTestFixtures.CatalogWith(fakeChooseCard.def);

        // Act
        var (next, _) = BattleEngine.PlayCard(state, handIndex: 0, null, null, Rng(), catalog);

        // Assert: pending なし、即時完了 (auto-skip で random 経路、hand 1 枚を exhaust)
        Assert.Null(next.PendingCardPlay);
    }

    [Fact]
    public void PlayCard_RegularCard_NoPending_CompletesAsBefore()
    {
        // Regression: choose 効果のないカード (Strike) は従来通り即時完了
        var state = ChooseTestFixtures.MinimalStateWithStrike();
        var catalog = BattleFixtures.MinimalCatalog(/* with strike */);
        var (next, _) = BattleEngine.PlayCard(state, handIndex: 0, null, null, Rng(),
            ChooseTestFixtures.MinimalCatalogWithStrike());
        Assert.Null(next.PendingCardPlay);
    }

    [Fact]
    public void PlayCard_WhilePending_Throws()
    {
        var state = ChooseTestFixtures.MinimalStateWithStrike() with {
            PendingCardPlay = new PendingCardPlay(
                "card_inst_x", 0, false,
                new PendingChoice("exhaustCard", "hand", 1, ImmutableArray<string>.Empty))
        };
        Assert.Throws<System.InvalidOperationException>(() =>
            BattleEngine.PlayCard(state, handIndex: 0, null, null, Rng(),
                ChooseTestFixtures.MinimalCatalogWithStrike()));
    }
}
```

`ChooseTestFixtures` は新ヘルパクラス (新規ファイル `tests/Core.Tests/Battle/Engine/ChooseTestFixtures.cs`):

```csharp
using System.Collections.Immutable;
using RoguelikeCardGame.Core.Battle.State;
using RoguelikeCardGame.Core.Cards;
using RoguelikeCardGame.Core.Data;
using RoguelikeCardGame.Core.Tests.Battle.Fixtures;

namespace RoguelikeCardGame.Core.Tests.Battle.Engine;

internal static class ChooseTestFixtures
{
    public static (string id, CardDefinition def) MakeExhaustCardChooseCard(string id, int amount)
    {
        var def = new CardDefinition(
            Id: id, Name: id, DisplayName: null,
            Rarity: CardRarity.Common, CardType: CardType.Skill,
            Cost: 0, UpgradedCost: null,
            Effects: new[] {
                new CardEffect("exhaustCard", EffectScope.Self, null, amount,
                               Pile: "hand", Select: "choose")
            },
            UpgradedEffects: null, Keywords: null, UpgradedKeywords: null,
            Description: null, UpgradedDescription: null);
        return (id, def);
    }

    public static BattleState StateWithHandAndCard(string[] hand, string chooseCardId)
    {
        var hero = BattleFixtures.Hero();
        var goblin = BattleFixtures.Goblin();
        var handCards = ImmutableArray.CreateRange(
            hand.Select((id, i) => new BattleCardInstance(
                InstanceId: $"card_inst_{i}", CardDefinitionId: id, IsUpgraded: false, CostOverride: null)));
        return new BattleState(
            Turn: 1, Phase: BattlePhase.PlayerInput,
            Outcome: BattleOutcome.Pending,
            Allies: ImmutableArray.Create(hero), Enemies: ImmutableArray.Create(goblin),
            TargetAllyIndex: 0, TargetEnemyIndex: 0,
            Energy: 3, EnergyMax: 3,
            DrawPile: ImmutableArray<BattleCardInstance>.Empty,
            Hand: handCards,
            DiscardPile: ImmutableArray<BattleCardInstance>.Empty,
            ExhaustPile: ImmutableArray<BattleCardInstance>.Empty,
            SummonHeld: ImmutableArray<BattleCardInstance>.Empty,
            PowerCards: ImmutableArray<BattleCardInstance>.Empty,
            ComboCount: 0, LastPlayedOrigCost: null, NextCardComboFreePass: false,
            OwnedRelicIds: ImmutableArray<string>.Empty,
            Potions: ImmutableArray<string>.Empty,
            EncounterId: "test_enc");
    }

    public static DataCatalog CatalogWith(CardDefinition def)
    {
        var baseCatalog = BattleFixtures.MinimalCatalog();
        var newCards = baseCatalog.Cards.ToDictionary(kv => kv.Key, kv => kv.Value);
        newCards[def.Id] = def;
        return baseCatalog with { Cards = newCards };
    }

    public static BattleState MinimalStateWithStrike()
    {
        // Strike-like minimal card for regression test
        var card = new CardDefinition(
            Id: "strike", Name: "Strike", DisplayName: null,
            Rarity: CardRarity.Common, CardType: CardType.Attack,
            Cost: 1, UpgradedCost: null,
            Effects: new[] { new CardEffect("attack", EffectScope.Single, EffectSide.Enemy, 6) },
            UpgradedEffects: null, Keywords: null, UpgradedKeywords: null,
            Description: null, UpgradedDescription: null);
        return StateWithHandAndCard(new[] { "strike" }, "strike");
    }

    public static DataCatalog MinimalCatalogWithStrike()
    {
        var card = new CardDefinition(
            Id: "strike", Name: "Strike", DisplayName: null,
            Rarity: CardRarity.Common, CardType: CardType.Attack,
            Cost: 1, UpgradedCost: null,
            Effects: new[] { new CardEffect("attack", EffectScope.Single, EffectSide.Enemy, 6) },
            UpgradedEffects: null, Keywords: null, UpgradedKeywords: null,
            Description: null, UpgradedDescription: null);
        return CatalogWith(card);
    }
}
```

(注: `BattleFixtures.MinimalCatalog()` の正確な signature は実装時に既存テストファイルで確認。`new BattleCardInstance(...)` の引数順は既存定義に合わせる — `InstanceId / CardDefinitionId / IsUpgraded / CostOverride` の順想定だが、実際の record 定義を読んで合わせる。)

- [ ] **Step 3.6: テスト失敗確認**

```bash
dotnet test tests/Core.Tests/Core.Tests.csproj --filter "FullyQualifiedName~BattleEngineChooseTests"
```
Expected: 4 件 FAIL (or compile error if API名前が違う等)。

- [ ] **Step 3.7: 全 Core テスト緑確認**

```bash
dotnet test tests/Core.Tests/Core.Tests.csproj
```
Expected: 既存 1251 + 新 4 = 1255 PASS。既存 PlayCard tests は無変更挙動 (choose 効果無いカードは pending null) で全部通過するはず。

- [ ] **Step 3.8: Commit + push**

```bash
git add src/Core/Battle/Engine/EffectApplier.cs src/Core/Battle/Engine/BattleEngine.PlayCard.cs tests/Core.Tests/Battle/Engine/BattleEngineChooseTests.cs tests/Core.Tests/Battle/Engine/ChooseTestFixtures.cs
git commit -m "$(cat <<'EOF'
feat(battle): emit PendingCardPlay on choose effect (Phase 10.5.M2-Choose T3)

ApplyEffectsFrom で choose effect (4 actions) を検出すると pending を return、
PlayCard も pending を見て即返却 (OnPlayCard / カード移動を skip)。
candidates ≤ Amount は auto-skip (random/all 経路に流れ、pending emit せず)。

EffectApplier に NeedsPlayerChoice / GetChooseCandidates / BuildPendingChoice
public helpers を追加 (T4 で resolve 側からも参照)。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push origin master
```

---

## Task 4: BattleEngine.ResolveCardChoice + ApplyChoseEffect (4 actions)

**Files:**
- Modify: `src/Core/Battle/Engine/BattleEngine.cs`
- Modify: `src/Core/Battle/Engine/EffectApplier.cs`
- Test: `tests/Core.Tests/Battle/Engine/BattleEngineChooseTests.cs` (resolve cases 追加)

- [ ] **Step 4.1: EffectApplier に ApplyChoseEffect 追加**

`src/Core/Battle/Engine/EffectApplier.cs` ファイル末尾 (helper section) に追加:

```csharp
    /// <summary>
    /// Phase 10.5.M2-Choose: 選択済 instance ids で choose effect を適用する (resume 経路)。
    /// 4 actions (discard / exhaustCard / upgrade / recoverFromDiscard) のみ対応。
    /// random / all モードの既存 helper を流用しつつ、対象を選択済 ids に限定して操作。
    /// </summary>
    public static (BattleState, IReadOnlyList<BattleEvent>) ApplyChoseEffect(
        BattleState state, CombatActor caster, CardEffect effect,
        ImmutableArray<string> selectedInstanceIds,
        IRng rng, DataCatalog catalog)
    {
        return effect.Action switch
        {
            "discard" => ApplyDiscardChosen(state, caster, selectedInstanceIds, rng, catalog),
            "exhaustCard" => ApplyExhaustCardChosen(state, caster, effect, selectedInstanceIds, rng, catalog),
            "upgrade" => ApplyUpgradeChosen(state, caster, effect, selectedInstanceIds, rng, catalog),
            "recoverFromDiscard" => ApplyRecoverFromDiscardChosen(state, caster, selectedInstanceIds, rng, catalog),
            _ => throw new System.InvalidOperationException(
                $"ApplyChoseEffect: action '{effect.Action}' does not support choose"),
        };
    }

    private static (BattleState, IReadOnlyList<BattleEvent>) ApplyDiscardChosen(
        BattleState state, CombatActor caster, ImmutableArray<string> selectedIds,
        IRng rng, DataCatalog catalog)
    {
        var hand = state.Hand.ToBuilder();
        var discard = state.DiscardPile.ToBuilder();
        int count = 0;
        foreach (var id in selectedIds)
        {
            int idx = -1;
            for (int i = 0; i < hand.Count; i++) { if (hand[i].InstanceId == id) { idx = i; break; } }
            if (idx < 0) continue;  // 既に消費済等 (validate 済前提だが念のため)
            discard.Add(hand[idx]);
            hand.RemoveAt(idx);
            count++;
        }
        var next = state with { Hand = hand.ToImmutable(), DiscardPile = discard.ToImmutable() };
        if (count == 0) return (next, System.Array.Empty<BattleEvent>());
        var events = new System.Collections.Generic.List<BattleEvent> {
            new BattleEvent(BattleEventKind.Discard, Order: 0,
                CasterInstanceId: caster.InstanceId, Amount: count, Note: "choose"),
        };
        return FireOnCardDiscarded(next, events, rng, catalog);
    }

    private static (BattleState, IReadOnlyList<BattleEvent>) ApplyExhaustCardChosen(
        BattleState state, CombatActor caster, CardEffect effect, ImmutableArray<string> selectedIds,
        IRng rng, DataCatalog catalog)
    {
        var (sourceBuilder, exhaustBuilder, applyResult) = OpenPile(state, effect.Pile);
        int count = 0;
        foreach (var id in selectedIds)
        {
            int idx = -1;
            for (int i = 0; i < sourceBuilder.Count; i++) { if (sourceBuilder[i].InstanceId == id) { idx = i; break; } }
            if (idx < 0) continue;
            exhaustBuilder.Add(sourceBuilder[idx]);
            sourceBuilder.RemoveAt(idx);
            count++;
        }
        var next = applyResult(sourceBuilder, exhaustBuilder);
        if (count == 0) return (next, System.Array.Empty<BattleEvent>());
        var events = new System.Collections.Generic.List<BattleEvent> {
            new BattleEvent(BattleEventKind.Exhaust, Order: 0,
                CasterInstanceId: caster.InstanceId, Amount: count, Note: effect.Pile),
        };
        return FireOnCardExhausted(next, events, rng, catalog);
    }

    private static (BattleState, IReadOnlyList<BattleEvent>) ApplyUpgradeChosen(
        BattleState state, CombatActor caster, CardEffect effect, ImmutableArray<string> selectedIds,
        IRng rng, DataCatalog catalog)
    {
        var (sourceBuilder, applyResult) = OpenSourcePile(state, effect.Pile);
        int upgraded = 0;
        foreach (var id in selectedIds)
        {
            for (int i = 0; i < sourceBuilder.Count; i++)
            {
                if (sourceBuilder[i].InstanceId != id) continue;
                if (sourceBuilder[i].IsUpgraded) break;
                if (!catalog.TryGetCard(sourceBuilder[i].CardDefinitionId, out var def)) break;
                if (!def.IsUpgradable) break;
                sourceBuilder[i] = sourceBuilder[i] with { IsUpgraded = true };
                upgraded++;
                break;
            }
        }
        var next = applyResult(sourceBuilder);
        if (upgraded == 0) return (next, System.Array.Empty<BattleEvent>());
        var events = new System.Collections.Generic.List<BattleEvent> {
            new BattleEvent(BattleEventKind.Upgrade, Order: 0,
                CasterInstanceId: caster.InstanceId, Amount: upgraded, Note: effect.Pile),
        };
        return (next, events);
    }

    private static (BattleState, IReadOnlyList<BattleEvent>) ApplyRecoverFromDiscardChosen(
        BattleState state, CombatActor caster, ImmutableArray<string> selectedIds,
        IRng rng, DataCatalog catalog)
    {
        var discard = state.DiscardPile.ToBuilder();
        var hand = state.Hand.ToBuilder();
        int count = 0;
        foreach (var id in selectedIds)
        {
            int idx = -1;
            for (int i = 0; i < discard.Count; i++) { if (discard[i].InstanceId == id) { idx = i; break; } }
            if (idx < 0) continue;
            hand.Add(discard[idx]);
            discard.RemoveAt(idx);
            count++;
        }
        var next = state with { Hand = hand.ToImmutable(), DiscardPile = discard.ToImmutable() };
        if (count == 0) return (next, System.Array.Empty<BattleEvent>());
        var events = new System.Collections.Generic.List<BattleEvent> {
            new BattleEvent(BattleEventKind.Recover, Order: 0,
                CasterInstanceId: caster.InstanceId, Amount: count, Note: "choose"),
        };
        return (next, events);
    }
```

(`OpenSourcePile` は既存 — 戻り値は `(builder, applyFn)` 形式。`BattleEventKind.Recover` / `Upgrade` の正確な enum 値は実装時に既存 EffectApplier 内 ApplyXxx を確認して合わせる。)

- [ ] **Step 4.2: BattleEngine.ResolveCardChoice 公開メソッド追加**

`src/Core/Battle/Engine/BattleEngine.cs` (or BattleEngine の partial にある場所、新ファイル `BattleEngine.ResolveChoice.cs` でも可) に追加:

```csharp
public static partial class BattleEngine
{
    /// <summary>
    /// Phase 10.5.M2-Choose: PendingCardPlay 状態から resume。
    /// 選択済 instance ids で choose effect を適用、残り effect を続行、必要なら再 pause。
    /// </summary>
    public static (BattleState, IReadOnlyList<BattleEvent>) ResolveCardChoice(
        BattleState state, ImmutableArray<string> selectedInstanceIds,
        IRng rng, DataCatalog catalog)
    {
        var pending = state.PendingCardPlay
            ?? throw new InvalidOperationException(
                "Cannot resolve card choice: no PendingCardPlay set");

        // validate selection
        if (selectedInstanceIds.Length != pending.Choice.Count)
            throw new InvalidOperationException(
                $"Expected {pending.Choice.Count} selections, got {selectedInstanceIds.Length}");
        foreach (var id in selectedInstanceIds)
        {
            if (!pending.Choice.CandidateInstanceIds.Contains(id))
                throw new InvalidOperationException(
                    $"Selected '{id}' not in choice candidates");
        }
        if (selectedInstanceIds.Distinct().Count() != selectedInstanceIds.Length)
            throw new InvalidOperationException("Duplicate selections not allowed");

        // pending 中のカード取得 (まだ Hand に居る前提)
        var card = state.Hand.FirstOrDefault(c => c.InstanceId == pending.CardInstanceId)
            ?? throw new InvalidOperationException(
                $"PendingCardPlay card '{pending.CardInstanceId}' not found in Hand");
        if (!catalog.TryGetCard(card.CardDefinitionId, out var def))
            throw new InvalidOperationException(
                $"PendingCardPlay card def '{card.CardDefinitionId}' not in catalog");

        var effects = (card.IsUpgraded && def.UpgradedEffects is not null)
            ? def.UpgradedEffects : def.Effects;
        var pendingEffect = effects[pending.EffectIndex];

        // pending クリア + choose effect 適用
        var s = state with { PendingCardPlay = null };
        var caster = s.Allies[0];
        var events = new List<BattleEvent>();
        int order = 0;

        var (afterChose, evsChose) = EffectApplier.ApplyChoseEffect(
            s, caster, pendingEffect, selectedInstanceIds, rng, catalog);
        s = afterChose;
        foreach (var ev in evsChose) events.Add(ev with { Order = order++ });
        caster = s.Allies[0];

        // 残り effect 続行 (再 pause 可能性あり)
        var (afterEffects, effectEvents, summonSucceeded, _) = ApplyEffectsFrom(
            s, caster, card, effects,
            startIndex: pending.EffectIndex + 1,
            newCombo: s.ComboCount,
            summonSucceededIn: pending.SummonSucceededBefore,
            ref order, rng, catalog);
        s = afterEffects;
        events.AddRange(effectEvents);

        // 再 pause していたら早期返却
        if (s.PendingCardPlay is not null)
            return (s, events);

        caster = s.Allies[0];

        // OnPlayCard relic + カード移動 + Power トリガ (PlayCard の終盤と同じロジック)
        // → PlayCard の対応セクションを共通 helper FinalizeCardPlay に切り出して呼ぶ
        return FinalizeCardPlay(s, caster, card, def, summonSucceeded, events, ref order, rng, catalog);
    }
}
```

`FinalizeCardPlay` は PlayCard 終盤 (OnPlayCard relic 発火 〜 OnCombo まで) を切り出した private helper。`BattleEngine.PlayCard.cs` 内に追加:

```csharp
    private static (BattleState, IReadOnlyList<BattleEvent>) FinalizeCardPlay(
        BattleState state, CombatActor caster, BattleCardInstance card, CardDefinition def,
        bool summonSucceeded, List<BattleEvent> events, ref int order,
        IRng rng, DataCatalog catalog)
    {
        var s = state;
        // OnPlayCard relic (effect 適用後・カード移動前)
        var (afterRelic, evsRelic) = RelicTriggerProcessor.Fire(s, "OnPlayCard", catalog, rng, orderStart: order);
        s = afterRelic;
        foreach (var ev in evsRelic) { events.Add(ev with { Order = order++ }); }

        // 5 段優先順位のカード移動
        var effects = (card.IsUpgraded && def.UpgradedEffects is not null) ? def.UpgradedEffects : def.Effects;
        bool hasExhaustSelf = effects.Any(e => e.Action == "exhaustSelf")
            || (def.EffectiveKeywords(card.IsUpgraded)?.Contains("exhaust") ?? false);
        bool hasRetainSelf = effects.Any(e => e.Action == "retainSelf")
            || (def.EffectiveKeywords(card.IsUpgraded)?.Contains("wait") ?? false);
        bool isPower = def.CardType == CardType.Power;
        bool isUnit = def.CardType == CardType.Unit;

        // hand から削除 (resume 経路でも card は依然 Hand に居る前提)
        int handIdx = -1;
        for (int i = 0; i < s.Hand.Length; i++) { if (s.Hand[i].InstanceId == card.InstanceId) { handIdx = i; break; } }
        if (handIdx >= 0) s = s with { Hand = s.Hand.RemoveAt(handIdx) };

        if (hasExhaustSelf)
            s = s with { ExhaustPile = s.ExhaustPile.Add(card) };
        else if (isPower)
            s = s with { PowerCards = s.PowerCards.Add(card) };
        else if (isUnit && summonSucceeded)
        {
            s = s with { SummonHeld = s.SummonHeld.Add(card) };
            int lastIdx = s.Allies.Length - 1;
            if (lastIdx >= 0
                && s.Allies[lastIdx].DefinitionId != "hero"
                && s.Allies[lastIdx].AssociatedSummonHeldInstanceId is null)
            {
                var summonActor = s.Allies[lastIdx];
                s = s with { Allies = s.Allies.SetItem(
                    lastIdx, summonActor with { AssociatedSummonHeldInstanceId = card.InstanceId }) };
            }
        }
        else if (hasRetainSelf)
        {
            // resume 経路の場合 handIdx 位置に戻す。元位置が分からなければ末尾追加。
            if (handIdx >= 0) s = s with { Hand = s.Hand.Insert(handIdx, card) };
            else s = s with { Hand = s.Hand.Add(card) };
        }
        else
            s = s with { DiscardPile = s.DiscardPile.Add(card) };

        var (afterOnPlay, evsOnPlay) = PowerTriggerProcessor.Fire(s, "OnPlayCard", catalog, rng, orderStart: order);
        s = afterOnPlay;
        foreach (var ev in evsOnPlay) { events.Add(ev with { Order = order++ }); }

        var (afterCombo, evsCombo) = PowerTriggerProcessor.FireOnCombo(s, s.ComboCount, catalog, rng, orderStart: order);
        s = afterCombo;
        foreach (var ev in evsCombo) { events.Add(ev with { Order = order++ }); }

        return (s, events);
    }
```

PlayCard 本体の対応セクション (line 127〜191 付近、OnPlayCard relic 発火〜) を `FinalizeCardPlay(s, caster, card, def, summonSucceeded, events, ref order, rng, catalog)` に置換。

- [ ] **Step 4.3: ResolveCardChoice テスト追加**

`tests/Core.Tests/Battle/Engine/BattleEngineChooseTests.cs` に追加:

```csharp
[Fact]
public void ResolveCardChoice_NoPending_Throws()
{
    var state = ChooseTestFixtures.MinimalStateWithStrike();
    Assert.Throws<System.InvalidOperationException>(() =>
        BattleEngine.ResolveCardChoice(state, ImmutableArray.Create("any"), Rng(),
            ChooseTestFixtures.MinimalCatalogWithStrike()));
}

[Fact]
public void ResolveCardChoice_WrongCount_Throws()
{
    var fakeChooseCard = ChooseTestFixtures.MakeExhaustCardChooseCard("ec", amount: 1);
    var state = ChooseTestFixtures.StateWithHandAndCard(
        new[] { "card_a", "card_b", "ec" }, "ec");
    var catalog = ChooseTestFixtures.CatalogWith(fakeChooseCard.def);
    var (paused, _) = BattleEngine.PlayCard(state, handIndex: 2, null, null, Rng(), catalog);
    Assert.NotNull(paused.PendingCardPlay);
    // count=1 expected, send 2
    Assert.Throws<System.InvalidOperationException>(() =>
        BattleEngine.ResolveCardChoice(paused,
            ImmutableArray.Create("card_inst_0", "card_inst_1"), Rng(), catalog));
}

[Fact]
public void ResolveCardChoice_NotInCandidates_Throws()
{
    var fakeChooseCard = ChooseTestFixtures.MakeExhaustCardChooseCard("ec", amount: 1);
    var state = ChooseTestFixtures.StateWithHandAndCard(
        new[] { "card_a", "card_b", "ec" }, "ec");
    var catalog = ChooseTestFixtures.CatalogWith(fakeChooseCard.def);
    var (paused, _) = BattleEngine.PlayCard(state, handIndex: 2, null, null, Rng(), catalog);
    Assert.Throws<System.InvalidOperationException>(() =>
        BattleEngine.ResolveCardChoice(paused, ImmutableArray.Create("card_inst_999"), Rng(), catalog));
}

[Fact]
public void ResolveCardChoice_Success_AppliesAndCompletes()
{
    var fakeChooseCard = ChooseTestFixtures.MakeExhaustCardChooseCard("ec", amount: 1);
    var state = ChooseTestFixtures.StateWithHandAndCard(
        new[] { "card_a", "card_b", "ec" }, "ec");
    var catalog = ChooseTestFixtures.CatalogWith(fakeChooseCard.def);
    var (paused, _) = BattleEngine.PlayCard(state, handIndex: 2, null, null, Rng(), catalog);

    var (final, _) = BattleEngine.ResolveCardChoice(paused,
        ImmutableArray.Create("card_inst_0"), Rng(), catalog);

    Assert.Null(final.PendingCardPlay);
    // card_a が exhaust に移動済
    Assert.Contains(final.ExhaustPile, c => c.InstanceId == "card_inst_0");
    Assert.DoesNotContain(final.Hand, c => c.InstanceId == "card_inst_0");
    // ec カード自身は discard へ (skill type、exhaustSelf 無し前提)
    Assert.Contains(final.DiscardPile, c => c.InstanceId == "ec");
}
```

- [ ] **Step 4.4: 全 Core テスト緑確認**

```bash
dotnet test tests/Core.Tests/Core.Tests.csproj
```
Expected: 1255 + 4 = 1259 PASS。

- [ ] **Step 4.5: Commit + push**

```bash
git add src/Core/Battle/Engine/BattleEngine.cs src/Core/Battle/Engine/BattleEngine.PlayCard.cs src/Core/Battle/Engine/EffectApplier.cs tests/Core.Tests/Battle/Engine/BattleEngineChooseTests.cs
git commit -m "$(cat <<'EOF'
feat(battle): ResolveCardChoice + ApplyChoseEffect for 4 actions (Phase 10.5.M2-Choose T4)

- BattleEngine.ResolveCardChoice: pending 状態から resume、選択 validate、
  ApplyChoseEffect で対象適用、残り effect 続行 (必要なら再 pause)
- EffectApplier.ApplyChoseEffect: discard / exhaustCard / upgrade /
  recoverFromDiscard の選択済 ids 適用
- PlayCard 終盤 (OnPlayCard relic / カード移動 / Power triggers) を
  FinalizeCardPlay private helper に切り出し、PlayCard と ResolveCardChoice 両方から呼ぶ

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push origin master
```

---

## Task 5: Server endpoint + DTO mapping

**Files:**
- Create: `src/Server/Dtos/PendingCardPlayDto.cs`
- Modify: `src/Server/Controllers/BattleController.cs` (`POST /battle/resolve-card-choice` 追加)
- Modify: `src/Server/Services/BattleStateDtoMapper.cs` (`PendingCardPlay` field マッピング)
- Test: `tests/Server.Tests/Controllers/BattleControllerResolveChoiceTests.cs` (新規)

- [ ] **Step 5.1: PendingCardPlayDto 新規作成**

`src/Server/Dtos/PendingCardPlayDto.cs`:
```csharp
namespace RoguelikeCardGame.Server.Dtos;

public sealed record PendingCardPlayDto(
    string CardInstanceId,
    int EffectIndex,
    PendingChoiceDto Choice);

public sealed record PendingChoiceDto(
    string Action,
    string Pile,
    int Count,
    string[] CandidateInstanceIds);

public sealed record ResolveCardChoiceRequestDto(
    string[] SelectedInstanceIds);
```

- [ ] **Step 5.2: BattleStateDtoMapper に PendingCardPlay マッピング追加**

`src/Server/Services/BattleStateDtoMapper.cs` の BattleState → DTO マッピング箇所 (return statement) に追加:
```csharp
PendingCardPlay: state.PendingCardPlay is null ? null : new PendingCardPlayDto(
    CardInstanceId: state.PendingCardPlay.CardInstanceId,
    EffectIndex: state.PendingCardPlay.EffectIndex,
    Choice: new PendingChoiceDto(
        Action: state.PendingCardPlay.Choice.Action,
        Pile: state.PendingCardPlay.Choice.Pile,
        Count: state.PendingCardPlay.Choice.Count,
        CandidateInstanceIds: state.PendingCardPlay.Choice.CandidateInstanceIds.ToArray())),
```

`BattleStateDto` record にも `PendingCardPlayDto? PendingCardPlay` を追加 (末尾、optional)。

- [ ] **Step 5.3: BattleController に endpoint 追加**

`src/Server/Controllers/BattleController.cs` の他 endpoint と同様 pattern で:
```csharp
[HttpPost("battle/resolve-card-choice")]
public async Task<IActionResult> ResolveCardChoice(
    [FromBody] ResolveCardChoiceRequestDto body, CancellationToken ct)
{
    var accountId = User.FindFirst("accountId")?.Value;  // (実際の auth pattern に合わせる)
    if (string.IsNullOrEmpty(accountId)) return Unauthorized();
    if (!_sessions.TryGet(accountId, out var session))
        return BadRequest("No active battle session");

    try
    {
        var (newState, events) = BattleEngine.ResolveCardChoice(
            session.State,
            body.SelectedInstanceIds.ToImmutableArray(),
            session.Rng, _data);
        _sessions.Update(accountId, newState);
        return Ok(BuildResponse(newState, events));  // (既存 PlayCard と同じ response 構築 helper を使う)
    }
    catch (System.InvalidOperationException ex)
    {
        return BadRequest(ex.Message);
    }
}
```

(`_sessions.TryGet` / `_sessions.Update` / `BuildResponse` は既存 PlayCard endpoint で使われている方法を grep で確認して合わせる。auth は project の既存 pattern を踏襲。)

- [ ] **Step 5.3a: 他 player-action endpoint に PendingCardPlay guard を追加**

`src/Server/Controllers/BattleController.cs` の以下 endpoint メソッド冒頭 (session 取得直後 / engine 呼出 直前) に共通 guard を挿入:
- `play-card` (line 125 付近) — 既に Engine 側 throw あるが、明示的 409 返却 で UX を統一
- `end-turn` (line 158 付近)
- `use-potion` (line 227 付近)
- `set-target` (line 192 付近)
- `finalize` (line 261 付近) — 防御的 (battle 終了後は通常 pending null だが念のため)

private static helper を BattleController に追加:
```csharp
/// <summary>
/// Phase 10.5.M2-Choose: PendingCardPlay (choose modal 待ち) 中は
/// 他 player action を全て reject する共通 guard。
/// </summary>
private static IActionResult? RejectIfPending(BattleSession session)
{
    if (session.State.PendingCardPlay is not null)
        return new ConflictObjectResult(new {
            error = "card_choice_pending",
            message = "Resolve the pending card choice before performing other actions.",
        });
    return null;
}
```

各 endpoint の session 取得直後に挿入:
```csharp
if (!_sessions.TryGet(accountId, out var session))
    return BadRequest("No active battle session");
if (RejectIfPending(session) is { } pendingResp) return pendingResp;
// ... 既存ロジック
```

(`resolve-card-choice` 自身には当然この guard を適用しない。`battle/start` (POST) と GET も session が存在しない/開始前なので不要。)

- [ ] **Step 5.4: Server test 追加**

新規 `tests/Server.Tests/Controllers/BattleControllerResolveChoiceTests.cs`:
- 1: pending なしで POST → 400
- 2: 不正 count → 400
- 3: 候補外 id → 400
- 4: 成功 → 200 + 新 state
- 5: 統合 (PlayCard で pending → resolve → 完了)

新規 `tests/Server.Tests/Controllers/BattleControllerPendingLockdownTests.cs` (action lockdown 検証):
- 6: pending 中に play-card POST → 409 (`card_choice_pending`)
- 7: pending 中に end-turn POST → 409
- 8: pending 中に use-potion POST → 409
- 9: pending 中に set-target POST → 409
- 10: pending 中に finalize POST → 409
- 11: pending を resolve した後は他 action が再び可能 (regression)

各テストは PlayCard で choose を発動 → pending 状態を作り、対象 endpoint を叩いて status を確認。`battle/start` POST や GET は session が無い/開始前なので blocked 対象外であることも確認 (任意で追加)。

(既存 `BattleControllerPlayCardTests.cs` の pattern を mirror)

- [ ] **Step 5.5: Server build OK 確認 (dev サーバ停止が必要)**

```bash
dotnet build src/Server/Server.csproj -c Release
dotnet test tests/Server.Tests/Server.Tests.csproj -c Release
```
Expected: 既存 278 + ResolveChoice 5 + Lockdown 6 = 289 PASS。

(Debug build で dev サーバ lock がある場合 Release で代替。)

- [ ] **Step 5.6: Commit + push**

```bash
git add src/Server/Dtos/PendingCardPlayDto.cs src/Server/Controllers/BattleController.cs src/Server/Services/BattleStateDtoMapper.cs tests/Server.Tests/Controllers/BattleControllerResolveChoiceTests.cs tests/Server.Tests/Controllers/BattleControllerPendingLockdownTests.cs
git commit -m "$(cat <<'EOF'
feat(battle-server): POST /battle/resolve-card-choice + pending lockdown (Phase 10.5.M2-Choose T5)

- PendingCardPlayDto / PendingChoiceDto / ResolveCardChoiceRequestDto 新規
- BattleStateDtoMapper で PendingCardPlay field をマッピング
- BattleController.ResolveCardChoice endpoint (POST、選択 ids body)
- 全 player-action endpoint (play-card / end-turn / use-potion / set-target / finalize)
  に PendingCardPlay guard (RejectIfPending) を追加、choose modal 待ち中は 409 で reject

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push origin master
```

---

## Task 6: Client DTO 型 + adapter

**Files:**
- Modify: `src/Client/src/api/types.ts` (`BattleStateDto.pendingCardPlay` 型)
- Create or Modify: `src/Client/src/api/battle.ts` (`resolveCardChoice` API client)
- Modify: `src/Client/src/screens/battleScreen/dtoAdapter.ts` (PendingCardPlay の型変換、必要なら)

- [ ] **Step 6.1: types.ts に PendingCardPlay 型追加**

`src/Client/src/api/types.ts` に追加:
```typescript
export type PendingCardPlayDto = {
  cardInstanceId: string
  effectIndex: number
  choice: {
    action: 'discard' | 'exhaustCard' | 'upgrade' | 'recoverFromDiscard'
    pile: 'hand' | 'draw' | 'discard'
    count: number
    candidateInstanceIds: string[]
  }
}
```

`BattleStateDto` 型 (既存) に `pendingCardPlay?: PendingCardPlayDto | null` field を追加。

- [ ] **Step 6.2: API client function**

`src/Client/src/api/battle.ts` (or 新規) に:
```typescript
import type { BattleStateDto } from './types'

export async function resolveCardChoice(
  selectedInstanceIds: string[],
): Promise<BattleStateDto> {
  const res = await fetch('/api/v1/runs/current/battle/resolve-card-choice', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ selectedInstanceIds }),
  })
  if (!res.ok) {
    const txt = await res.text()
    throw new Error(`resolveCardChoice failed: ${res.status} ${txt}`)
  }
  return await res.json()
}
```

(認証 header / accountId は project の既存 fetch pattern を踏襲。)

- [ ] **Step 6.3: dtoAdapter (もし adapter 経由で battle state を変換しているなら) PendingCardPlay 通過確認**

既存 `dtoAdapter.ts` で BattleState を CharacterDemo / Hand 等に変換しているが、PendingCardPlay は modal 用に raw のまま使えれば adapter 変更不要 (BattleScreen で直接 read)。

- [ ] **Step 6.4: tsc + Client tests**

```bash
cd src/Client && npx tsc --noEmit && npm run test:run
```
Expected: regression なし、既存全 PASS。

- [ ] **Step 6.5: Commit + push**

```bash
git add src/Client/src/api/types.ts src/Client/src/api/battle.ts
git commit -m "$(cat <<'EOF'
feat(client): pendingCardPlay DTO type + resolveCardChoice API client (Phase 10.5.M2-Choose T6)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push origin master
```

---

## Task 7: CardChoiceModal — Hand mode

**Files:**
- Create: `src/Client/src/screens/battleScreen/CardChoiceModal.tsx`
- Create: `src/Client/src/screens/battleScreen/CardChoiceModal.css`
- Modify: `src/Client/src/screens/BattleScreen.tsx` (modal 統合)
- Test: `src/Client/src/screens/battleScreen/CardChoiceModal.test.tsx`

**Goal:** `pile === 'hand'` ケース。手札 UI を流用、選択可能カードを枠ハイライト、N 枚選択で確定。

- [ ] **Step 7.1: CardChoiceModal.tsx 新規作成 (Hand mode 専用、List mode は T8 で追加)**

```tsx
import { useState } from 'react'
import { Popup } from '../../components/Popup'
import { Button } from '../../components/Button'
import { Card } from '../../components/Card'
import type { PendingCardPlayDto } from '../../api/types'
import './CardChoiceModal.css'

type Props = {
  pending: PendingCardPlayDto
  hand: { instanceId: string; cardDefinitionId: string; isUpgraded: boolean }[]
  // draw/discard は List mode 用、T8 で利用
  drawPile?: { instanceId: string; cardDefinitionId: string; isUpgraded: boolean }[]
  discardPile?: { instanceId: string; cardDefinitionId: string; isUpgraded: boolean }[]
  cardNames: Record<string, string>
  onConfirm: (selectedIds: string[]) => Promise<void>
}

export function CardChoiceModal({ pending, hand, cardNames, onConfirm }: Props) {
  const [selected, setSelected] = useState<string[]>([])
  const [busy, setBusy] = useState(false)
  const choice = pending.choice

  const isCandidate = (id: string) => choice.candidateInstanceIds.includes(id)
  const isSelected = (id: string) => selected.includes(id)

  const toggle = (id: string) => {
    if (busy || !isCandidate(id)) return
    setSelected(prev => {
      if (prev.includes(id)) return prev.filter(x => x !== id)
      if (prev.length >= choice.count) return prev  // 上限
      return [...prev, id]
    })
  }

  const handleConfirm = async () => {
    if (selected.length !== choice.count || busy) return
    setBusy(true)
    try { await onConfirm(selected) } finally { setBusy(false) }
  }

  // Hand mode のみ T7 で実装、List mode は T8
  if (choice.pile !== 'hand') return null

  const title = actionTitle(choice.action) + ` (${selected.length}/${choice.count})`

  return (
    <Popup
      open
      variant="modal"
      title={title}
      width={1200}
      closeOnEsc={false}
      footerAlign="center"
      footer={
        <Button onClick={() => void handleConfirm()} disabled={selected.length !== choice.count || busy}>
          確定
        </Button>
      }
    >
      <div className="ccm-hand">
        {hand.map(c => {
          const cand = isCandidate(c.instanceId)
          const sel = isSelected(c.instanceId)
          const cls = ['ccm-hand__card', cand ? 'is-candidate' : 'is-disabled', sel ? 'is-selected' : '']
            .filter(Boolean).join(' ')
          return (
            <div key={c.instanceId} className={cls} onClick={() => toggle(c.instanceId)}>
              <Card
                name={cardNames[c.cardDefinitionId] ?? c.cardDefinitionId}
                cost={null} description={null} cardType={null} rarity={null}
                upgraded={c.isUpgraded}
              />
            </div>
          )
        })}
      </div>
    </Popup>
  )
}

function actionTitle(action: string): string {
  switch (action) {
    case 'discard':            return 'カードを捨てる'
    case 'exhaustCard':        return 'カードを除外する'
    case 'upgrade':            return 'カードを強化する'
    case 'recoverFromDiscard': return 'カードを手札に戻す'
    default: return 'カードを選ぶ'
  }
}
```

CSS (`CardChoiceModal.css`):
```css
.ccm-hand {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  justify-content: center;
  padding: 16px;
}

.ccm-hand__card {
  cursor: pointer;
  transition: transform 0.15s, opacity 0.15s, box-shadow 0.15s;
  border-radius: 8px;
}

.ccm-hand__card.is-candidate {
  box-shadow: 0 0 0 2px rgba(255, 210, 63, 0.6);
}

.ccm-hand__card.is-disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.ccm-hand__card.is-selected {
  transform: translateY(-8px);
  box-shadow: 0 0 0 4px #ffd23f, 0 8px 16px rgba(0, 0, 0, 0.5);
}
```

(Card コンポーネントのプロパティは既存 `<Card>` の signature に合わせる — 実装時に既存使用箇所を grep で確認。`name / cost / description / cardType / rarity / upgraded` 想定だが、null 可否を確認。)

- [ ] **Step 7.2: BattleScreen 統合 + 他 UI control の lockdown**

`src/Client/src/screens/BattleScreen.tsx` で battle state から pending を取り出し、non-null なら modal 表示:

```tsx
const isPending = state.pendingCardPlay != null
// ...
{isPending && (
  <CardChoiceModal
    pending={state.pendingCardPlay!}
    hand={state.hand}
    drawPile={state.drawPile}
    discardPile={state.discardPile}
    cardNames={cardNames}
    onConfirm={async (ids) => {
      const newState = await resolveCardChoice(ids)
      setState(newState)
    }}
  />
)}
```

(`setState(newState)` は battle state 更新の既存 setter に合わせる。)

**Lockdown: pending 中は他 UI control を disable する。** `isPending` を以下に伝搬:

1. **手札カードクリック** (PlayCard 起動): hand area 側で `onClick` を `isPending` 時に no-op 化、または `pointer-events: none` クラス追加。
   ```tsx
   <div className={`battle-hand ${isPending ? 'is-locked' : ''}`}
        onClick={isPending ? undefined : handleHandClick}>
   ```
   CSS: `.battle-hand.is-locked { pointer-events: none; opacity: 0.6; }`

2. **ポーション buttons** (UsePotion): 既存 potion ボタンに `disabled={isPending || ...}` を追加。
3. **End Turn button**: 既存 end-turn ボタンに `disabled={isPending || ...}` を追加。
4. **ターゲット選択** (SetTarget): 敵クリック handler を `isPending` 時に no-op 化、視覚的にも `cursor: not-allowed` 等で示す。
5. **その他 BattleScreen 内 player-action UI** があれば同様に。

(具体ファイル: `BattleScreen.tsx` 内で hand / potion / end-turn / target を render している場所を grep して各々に `isPending` ガードを入れる。実装時に `BattleScreen.tsx` を読んで対象 UI を列挙する。)

modal 自身は `closeOnEsc={false}` (既に T7.1 で設定済) かつ「キャンセルボタンなし」なので、ユーザは選択して確定する以外に escape できず、UX 的に 1 アクションだけが許可された状態になる。

- [ ] **Step 7.3: Client test (Hand mode 基本動作)**

`src/Client/src/screens/battleScreen/CardChoiceModal.test.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import { CardChoiceModal } from './CardChoiceModal'

const basePending = {
  cardInstanceId: 'play_card',
  effectIndex: 0,
  choice: {
    action: 'exhaustCard' as const,
    pile: 'hand' as const,
    count: 1,
    candidateInstanceIds: ['c1', 'c2'],
  },
}

const baseHand = [
  { instanceId: 'c1', cardDefinitionId: 'strike', isUpgraded: false },
  { instanceId: 'c2', cardDefinitionId: 'defend', isUpgraded: false },
  { instanceId: 'play_card', cardDefinitionId: 'choose_card', isUpgraded: false },  // not in candidates
]

describe('CardChoiceModal Hand mode', () => {
  it('shows confirm button disabled until N selected', () => {
    render(
      <CardChoiceModal pending={basePending} hand={baseHand}
        cardNames={{ strike: 'Strike', defend: 'Defend' }}
        onConfirm={vi.fn()} />
    )
    const btn = screen.getByText('確定') as HTMLButtonElement
    expect(btn.disabled).toBe(true)
  })

  it('selecting candidate enables confirm', () => {
    render(
      <CardChoiceModal pending={basePending} hand={baseHand}
        cardNames={{ strike: 'Strike', defend: 'Defend' }}
        onConfirm={vi.fn()} />
    )
    fireEvent.click(screen.getByText('Strike'))  // c1 = candidate
    const btn = screen.getByText('確定') as HTMLButtonElement
    expect(btn.disabled).toBe(false)
  })

  it('confirm calls onConfirm with selected ids', async () => {
    const onConfirm = vi.fn().mockResolvedValue(undefined)
    render(
      <CardChoiceModal pending={basePending} hand={baseHand}
        cardNames={{ strike: 'Strike', defend: 'Defend' }}
        onConfirm={onConfirm} />
    )
    fireEvent.click(screen.getByText('Strike'))
    fireEvent.click(screen.getByText('確定'))
    await waitFor(() => expect(onConfirm).toHaveBeenCalledWith(['c1']))
  })
})
```

`src/Client/src/screens/BattleScreen.test.tsx` (既存ファイルがあれば追記、なければ簡易版を新規) に lockdown 検証 test を追加:

```tsx
describe('BattleScreen lockdown while pendingCardPlay', () => {
  it('disables hand clicks / potion / end-turn while pending', () => {
    // pending 状態の battle state を mock し BattleScreen を render、
    // 手札カードを click しても play-card API が呼ばれないこと、
    // ポーションボタン / end-turn ボタンが disabled であることを確認
    // (具体的な mock は既存 BattleScreen テストのヘルパに合わせる)
  })
})
```

(BattleScreen 単体テストが project に既にあるかは実装時に確認、無ければ skip 可 — Server 側 6 件の lockdown test で十分カバーできる。)

- [ ] **Step 7.4: tsc + Client tests**

```bash
cd src/Client && npx tsc --noEmit && npm run test:run
```
Expected: 既存 + Hand mode 3 + (lockdown 1, 任意) PASS。

- [ ] **Step 7.5: Commit + push**

```bash
git add src/Client/src/screens/battleScreen/CardChoiceModal.tsx src/Client/src/screens/battleScreen/CardChoiceModal.css src/Client/src/screens/battleScreen/CardChoiceModal.test.tsx src/Client/src/screens/BattleScreen.tsx
git commit -m "$(cat <<'EOF'
feat(client): CardChoiceModal Hand mode + pending UI lockdown (Phase 10.5.M2-Choose T7)

pile=hand 用の手札選択 UI。候補カードを枠ハイライト、N 枚選択で
「確定」ボタン enable。BattleScreen に統合し pendingCardPlay 検出時に表示。
さらに pendingCardPlay 中は手札クリック / ポーション / end-turn / ターゲット選択を全て disable し、
modal 操作のみ受け付けるようにした (Server 側 RejectIfPending と二重防御)。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push origin master
```

---

## Task 8: CardChoiceModal — List mode (draw/discard pile)

**Files:**
- Modify: `src/Client/src/screens/battleScreen/CardChoiceModal.tsx` (List mode 追加)
- Modify: `src/Client/src/screens/battleScreen/CardChoiceModal.css` (List mode スタイル)
- Test: 既存 `CardChoiceModal.test.tsx` に List mode test 追加

- [ ] **Step 8.1: CardChoiceModal に List mode 分岐追加**

`if (choice.pile !== 'hand') return null` を削除し、List mode 描画を追加:

```tsx
  // 候補プール取得 (pile に応じて)
  const sourcePile =
    choice.pile === 'hand' ? hand
    : choice.pile === 'draw' ? (drawPile ?? [])
    : choice.pile === 'discard' ? (discardPile ?? [])
    : []

  // ... title 構築 共通化 ...

  if (choice.pile === 'hand') {
    return ( /* 既存 Hand mode return */ )
  }

  // List mode (draw / discard)
  return (
    <Popup open variant="modal" title={title} width={1000}
           closeOnEsc={false} footerAlign="center"
           footer={<Button onClick={() => void handleConfirm()} disabled={selected.length !== choice.count || busy}>確定</Button>}>
      <div className="ccm-list">
        {sourcePile.map(c => {
          const cand = isCandidate(c.instanceId)
          const sel = isSelected(c.instanceId)
          const cls = ['ccm-list__row', cand ? 'is-candidate' : 'is-disabled', sel ? 'is-selected' : '']
            .filter(Boolean).join(' ')
          return (
            <div key={c.instanceId} className={cls} onClick={() => toggle(c.instanceId)}>
              <span className="ccm-list__name">
                {cardNames[c.cardDefinitionId] ?? c.cardDefinitionId}
                {c.isUpgraded ? '+' : ''}
              </span>
            </div>
          )
        })}
      </div>
    </Popup>
  )
```

CSS 追加:
```css
.ccm-list {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: 16px;
  max-height: 60vh;
  overflow-y: auto;
}

.ccm-list__row {
  padding: 8px 12px;
  border: 1px solid #444;
  background: #1a1a1a;
  border-radius: 4px;
  cursor: pointer;
  font-size: 14px;
  transition: background 0.15s, border 0.15s;
}

.ccm-list__row.is-candidate:hover {
  background: #2a2a2a;
}

.ccm-list__row.is-disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.ccm-list__row.is-selected {
  background: #3a3a1a;
  border-color: #ffd23f;
  color: #ffd23f;
}
```

- [ ] **Step 8.2: List mode test 追加**

`CardChoiceModal.test.tsx` に追加:
```tsx
const drawPending = {
  cardInstanceId: 'play_card', effectIndex: 0,
  choice: {
    action: 'exhaustCard' as const, pile: 'draw' as const,
    count: 1, candidateInstanceIds: ['d1', 'd2'],
  },
}
const drawPile = [
  { instanceId: 'd1', cardDefinitionId: 'fire', isUpgraded: false },
  { instanceId: 'd2', cardDefinitionId: 'ice', isUpgraded: true },
]

describe('CardChoiceModal List mode', () => {
  it('shows draw pile cards as list rows', () => {
    render(
      <CardChoiceModal pending={drawPending} hand={[]} drawPile={drawPile}
        cardNames={{ fire: '炎', ice: '氷' }}
        onConfirm={vi.fn()} />
    )
    expect(screen.getByText('炎')).toBeTruthy()
    expect(screen.getByText('氷+')).toBeTruthy()  // upgraded
  })

  it('clicking row selects, confirm sends id', async () => {
    const onConfirm = vi.fn().mockResolvedValue(undefined)
    render(
      <CardChoiceModal pending={drawPending} hand={[]} drawPile={drawPile}
        cardNames={{ fire: '炎', ice: '氷' }}
        onConfirm={onConfirm} />
    )
    fireEvent.click(screen.getByText('炎'))
    fireEvent.click(screen.getByText('確定'))
    await waitFor(() => expect(onConfirm).toHaveBeenCalledWith(['d1']))
  })
})
```

- [ ] **Step 8.3: tsc + Client tests**

```bash
cd src/Client && npx tsc --noEmit && npm run test:run
```
Expected: 既存 + Hand mode 3 + List mode 2 = +5 件 PASS。

- [ ] **Step 8.4: Commit + push**

```bash
git add src/Client/src/screens/battleScreen/CardChoiceModal.tsx src/Client/src/screens/battleScreen/CardChoiceModal.css src/Client/src/screens/battleScreen/CardChoiceModal.test.tsx
git commit -m "$(cat <<'EOF'
feat(client): CardChoiceModal List mode for draw/discard pile (Phase 10.5.M2-Choose T8)

pile=draw|discard 用のリスト UI。各カード名を縦並びで表示、
クリックで選択 / 確定で onConfirm(ids) call。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push origin master
```

---

## Task 9: 統合テスト + memory + push

**Files:** memory のみ (production code 変更なし)

- [ ] **Step 9.1: 全テスト緑確認**

```bash
dotnet test tests/Core.Tests/Core.Tests.csproj
dotnet test tests/Server.Tests/Server.Tests.csproj -c Release
cd src/Client && npx tsc --noEmit && npm run test:run
```

Expected:
- Core: 1259 PASS (1251 + 8 new)
- Server: 289 PASS (278 + 5 ResolveChoice + 6 Lockdown)
- Client: 182 PASS (177 + 5 modal Hand+List) ※ BattleScreen lockdown test を追加した場合は +1
- tsc clean

- [ ] **Step 9.2: 動作確認 (任意推奨)**

`dev.bat` で server + client 起動。dev menu で choose 系カード (例: exhaustCard select=choose, pile=hand, amount=1) を fake card で作成 → 戦闘で プレイ → modal 表示 + 選択 + 確定で正しく動くか実プレイで確認。

- [ ] **Step 9.3: Memory 更新**

`C:\Users\Metaverse\.claude\projects\c--Users-Metaverse-projects-roguelike-cardgame\memory\project_phase_status.md` を Phase 10.5.M2-Choose 完了状態に更新:
```markdown
- **Phase 10.5.M2-Choose: UI card selection modal** (T1〜T9 完了、master 直接 commit + push)
  - PendingCardPlay / PendingChoice records + BattleState field
  - BattleEngine.PlayCard を per-effect state machine に refactor (pause-resume 基盤)
  - 4 actions (discard / exhaustCard / upgrade / recoverFromDiscard) で choose 検出 + 適用
  - BattleEngine.ResolveCardChoice + ApplyChoseEffect
  - Server endpoint POST /battle/resolve-card-choice
  - 全 player-action endpoint で PendingCardPlay 中は 409 reject (lockdown)
  - Client CardChoiceModal (Hand mode + List mode) + pending 中は他 UI control 全 disable
  - 複合 choose は順次 pause、N >= candidates なら auto-skip
  - テスト: 1251 → 1259 (+8 Core)、278 → 289 (+11 Server)、177 → 182+ (+5 Client)
```

`MEMORY.md` の最初行を更新:
```markdown
- [フェーズ進捗 (2026-05-08)](project_phase_status.md) — Phase 10.5.M2-Choose (UI card selection modal) 完了。次は 10.6.B-Reroll N 回 / 10.5.L2 / Phase 9
```

(memory ファイルは git 管理外なので commit 不要。)

- [ ] **Step 9.4: Final push 確認**

```bash
git status --short
git log --oneline origin/master..HEAD
```
Expected: working tree clean、未 push commit 無し。

---

## Out of Scope (本 phase 非対応、将来検討)

- **キャンセル機構**: ユーザ要望次第で別 phase で B 案 (effect だけスキップ) を実装可能
- **「up to N 枚」モード**: 「最大 N 枚選んでよい」(0〜N) — 現状は exact N
- **Multi-player choose 同期**: Phase 9 (multiplayer) 着手時に「他プレイヤーの choose 中は待機」設計が必要
- **AI 選択シミュレーション**: 自動プレイ時の選択ロジック
- **カードプレイ前 preview**: 選択前の効果プレビューモーダル
