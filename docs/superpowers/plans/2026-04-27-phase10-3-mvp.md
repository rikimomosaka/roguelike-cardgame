# Phase 10.3-MVP — REST + in-memory 最小プレイアブル戦闘 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Phase 10.2 完成の `BattleEngine` を REST + in-memory で実プレイ可能にする。Server に `BattleController` 7 endpoint + `BattleSessionStore` + フル `BattleStateDto`、Client は既存 `BattleScreen.tsx` (battle-v10.html ポート済) を API 接続化、`BattleOverlay.tsx` 退役。

**Architecture:** REST 各アクション (`start/get/play-card/end-turn/use-potion/set-target/finalize`) で `(state, events)` 一括返却。戦闘 state は `BattleSessionStore` (`ConcurrentDictionary<accountId, BattleState>`) で in-memory 保持、リロード時は同 encounter で再開始。`BattleStateDto` フル DTO + AttackPool/BlockPool は Display 値計算済、Client は events を 200ms ステップで順次再生 (CSS transition で数値補間)。

**Tech Stack:** ASP.NET Core 10 (REST controller) / xUnit (Server tests) / React 19 + TypeScript + Vite (Client) / `BattleEngine` Core API (10.2 完成)

**Spec:** [`../specs/2026-04-27-phase10-3-mvp-design.md`](../specs/2026-04-27-phase10-3-mvp-design.md)

---

## Conventions

- 全 commit 末尾に `Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>` を含める（HEREDOC 使用）
- `dotnet build` 警告 0 / エラー 0 を全タスク完了時に維持
- `dotnet test` 緑をタスク完了時に維持
- `BattleOutcome` 型は `RoguelikeCardGame.Core.Battle.State.BattleOutcome.X` で fully qualified（memory feedback）
- `state.Allies` / `state.Enemies` への書き戻しは InstanceId 検索（`BattleController` は委譲のみで該当なし）
- 各タスク完了で commit + push (Option B: タスク単位 auto commit)

---

## Task 0: 既存 BattleStateDto を BattlePlaceholderStateDto にリネーム

**目的:** spec §3-7 で確定、新フル `BattleStateDto` を Task 1 で導入するため、既存 placeholder 用 DTO を別名に退避。

**Files:**
- Rename: `src/Server/Dtos/BattleStateDto.cs` → 内部の `record BattleStateDto` / `EnemyInstanceDto` をそれぞれ `BattlePlaceholderStateDto` / `PlaceholderEnemyInstanceDto` に rename。ファイル名も `src/Server/Dtos/BattlePlaceholderStateDto.cs` に rename
- Modify: `src/Server/Dtos/RunSnapshotDto.cs` (line 27, 56-66): `BattleStateDto?` → `BattlePlaceholderStateDto?`、`EnemyInstanceDto` → `PlaceholderEnemyInstanceDto`
- Modify: `src/Client/src/api/types.ts` (line 65-78): TypeScript 型 `BattleStateDto` / `EnemyInstanceDto` → `BattlePlaceholderStateDto` / `PlaceholderEnemyInstanceDto`、`RunStateDto.activeBattle` 型も追従
- Modify: `src/Client/src/screens/BattleOverlay.tsx`: import を `BattlePlaceholderStateDto` に変更
- Modify: `src/Client/src/screens/BattleOverlay.test.tsx`: 同
- Modify: `src/Client/src/screens/MapScreen.test.tsx`: 同（grep で `BattleStateDto` 参照箇所を更新）

- [ ] **Step 1: Server 側ファイル rename**

```bash
git mv src/Server/Dtos/BattleStateDto.cs src/Server/Dtos/BattlePlaceholderStateDto.cs
```

ファイルの中身を以下に書き換え:

```csharp
using System.Collections.Generic;

namespace RoguelikeCardGame.Server.Dtos;

public sealed record BattlePlaceholderStateDto(
    string EncounterId,
    IReadOnlyList<PlaceholderEnemyInstanceDto> Enemies,
    string Outcome);

public sealed record PlaceholderEnemyInstanceDto(
    string EnemyDefinitionId,
    string Name,
    string ImageId,
    int CurrentHp,
    int MaxHp,
    string CurrentMoveId);
```

- [ ] **Step 2: `RunSnapshotDto.cs` の参照更新**

`src/Server/Dtos/RunSnapshotDto.cs:27` を:
```csharp
BattlePlaceholderStateDto? ActiveBattle,
```

`src/Server/Dtos/RunSnapshotDto.cs:56-66` (Mapper 内) を:
```csharp
BattlePlaceholderStateDto? battle = null;
if (s.ActiveBattle is { } b)
{
    var enemies = new List<PlaceholderEnemyInstanceDto>();
    foreach (var e in b.Enemies)
    {
        var def = data.Enemies[e.EnemyDefinitionId];
        enemies.Add(new PlaceholderEnemyInstanceDto(e.EnemyDefinitionId, def.Name, def.ImageId,
            e.CurrentHp, e.MaxHp, e.CurrentMoveId));
    }
    battle = new BattlePlaceholderStateDto(b.EncounterId, enemies, b.Outcome.ToString());
}
```

- [ ] **Step 3: Client TypeScript 型更新**

`src/Client/src/api/types.ts:65-78` を:
```typescript
export type PlaceholderEnemyInstanceDto = {
  enemyDefinitionId: string
  name: string
  imageId: string
  currentHp: number
  maxHp: number
  currentMoveId: string
}

export type BattlePlaceholderStateDto = {
  encounterId: string
  enemies: PlaceholderEnemyInstanceDto[]
  outcome: BattleOutcome
}
```

`RunStateDto` の中の `activeBattle` field 型を `BattlePlaceholderStateDto | null` に更新（既存名を grep して更新）。

- [ ] **Step 4: Client side 残り参照更新**

```bash
grep -rn "BattleStateDto\|EnemyInstanceDto" src/Client
```
で残った箇所 (`BattleOverlay.tsx` / `BattleOverlay.test.tsx` / `MapScreen.test.tsx`) を `BattlePlaceholderStateDto` / `PlaceholderEnemyInstanceDto` に置換。

- [ ] **Step 5: Build + test 確認**

```bash
dotnet build
dotnet test
cd src/Client && npm run build && cd ../..
```
Expected: 全テスト緑、build 0 警告

- [ ] **Step 6: Commit + push**

```bash
git add src/Server/Dtos/BattlePlaceholderStateDto.cs src/Server/Dtos/RunSnapshotDto.cs src/Client/src/api/types.ts src/Client/src/screens/BattleOverlay.tsx src/Client/src/screens/BattleOverlay.test.tsx src/Client/src/screens/MapScreen.test.tsx
git rm src/Server/Dtos/BattleStateDto.cs   # rename されているはずだが念のため
git commit -m "$(cat <<'EOF'
refactor(server): rename BattleStateDto to BattlePlaceholderStateDto (Phase 10.3-MVP Task 0)

spec §3-7: 新フル BattleStateDto (Task 1 で導入) との衝突回避のため、
既存 placeholder 用 DTO を BattlePlaceholderStateDto / PlaceholderEnemyInstanceDto
に rename。RunSnapshotDto / Client types.ts / BattleOverlay 関連の参照を追従更新。
Phase 10.5 で placeholder 退役時に削除予定。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 1: 新 DTO 群を追加

**目的:** spec §3 で定義された全 DTO（`BattleStateDto` / `CombatActorDto` / `BattleCardInstanceDto` / `BattleEventDto` / `BattleEventStepDto` / `BattleActionResponseDto` + 5 リクエスト DTO）を新規追加。

**Files:**
- Create: `src/Server/Dtos/BattleStateDto.cs` (フル 新型)
- Create: `src/Server/Dtos/CombatActorDto.cs`
- Create: `src/Server/Dtos/BattleCardInstanceDto.cs`
- Create: `src/Server/Dtos/BattleEventDto.cs`
- Create: `src/Server/Dtos/BattleEventStepDto.cs`
- Create: `src/Server/Dtos/BattleActionResponseDto.cs`
- Create: `src/Server/Dtos/BattleStartRequestDto.cs`
- Create: `src/Server/Dtos/EndTurnRequestDto.cs`
- Create: `src/Server/Dtos/PlayCardRequestDto.cs`
- Create: `src/Server/Dtos/UsePotionRequestDto.cs`
- Create: `src/Server/Dtos/SetTargetRequestDto.cs`

- [ ] **Step 1: 新 `BattleStateDto.cs` 作成**

`src/Server/Dtos/BattleStateDto.cs`:
```csharp
using System.Collections.Generic;

namespace RoguelikeCardGame.Server.Dtos;

public sealed record BattleStateDto(
    int Turn,
    string Phase,
    string Outcome,
    IReadOnlyList<CombatActorDto> Allies,
    IReadOnlyList<CombatActorDto> Enemies,
    int? TargetAllyIndex,
    int? TargetEnemyIndex,
    int Energy,
    int EnergyMax,
    IReadOnlyList<BattleCardInstanceDto> DrawPile,
    IReadOnlyList<BattleCardInstanceDto> Hand,
    IReadOnlyList<BattleCardInstanceDto> DiscardPile,
    IReadOnlyList<BattleCardInstanceDto> ExhaustPile,
    IReadOnlyList<BattleCardInstanceDto> SummonHeld,
    IReadOnlyList<BattleCardInstanceDto> PowerCards,
    int ComboCount,
    int? LastPlayedOrigCost,
    bool NextCardComboFreePass,
    IReadOnlyList<string> OwnedRelicIds,
    IReadOnlyList<string> Potions,
    string EncounterId);
```

- [ ] **Step 2: `CombatActorDto.cs` 作成**

`src/Server/Dtos/CombatActorDto.cs`:
```csharp
using System.Collections.Generic;

namespace RoguelikeCardGame.Server.Dtos;

public sealed record CombatActorDto(
    string InstanceId,
    string DefinitionId,
    string Side,
    int SlotIndex,
    int CurrentHp,
    int MaxHp,
    int BlockDisplay,
    int AttackSingleDisplay,
    int AttackRandomDisplay,
    int AttackAllDisplay,
    IReadOnlyDictionary<string, int> Statuses,
    string? CurrentMoveId,
    int? RemainingLifetimeTurns,
    string? AssociatedSummonHeldInstanceId);
```

- [ ] **Step 3: `BattleCardInstanceDto.cs` 作成**

`src/Server/Dtos/BattleCardInstanceDto.cs`:
```csharp
namespace RoguelikeCardGame.Server.Dtos;

public sealed record BattleCardInstanceDto(
    string InstanceId,
    string CardDefinitionId,
    bool IsUpgraded,
    int? CostOverride);
```

- [ ] **Step 4: `BattleEventDto.cs` 作成**

`src/Server/Dtos/BattleEventDto.cs`:
```csharp
namespace RoguelikeCardGame.Server.Dtos;

public sealed record BattleEventDto(
    string Kind,
    int Order,
    string? CasterInstanceId,
    string? TargetInstanceId,
    int? Amount,
    string? CardId,
    string? Note);
```

- [ ] **Step 5: `BattleEventStepDto.cs` + `BattleActionResponseDto.cs` 作成**

`src/Server/Dtos/BattleEventStepDto.cs`:
```csharp
namespace RoguelikeCardGame.Server.Dtos;

public sealed record BattleEventStepDto(
    BattleEventDto Event,
    BattleStateDto SnapshotAfter);
```

`src/Server/Dtos/BattleActionResponseDto.cs`:
```csharp
using System.Collections.Generic;

namespace RoguelikeCardGame.Server.Dtos;

public sealed record BattleActionResponseDto(
    BattleStateDto State,
    IReadOnlyList<BattleEventStepDto> Steps);
```

- [ ] **Step 6: 5 リクエスト DTO 作成**

`src/Server/Dtos/BattleStartRequestDto.cs`:
```csharp
namespace RoguelikeCardGame.Server.Dtos;

public sealed record BattleStartRequestDto();
```

`src/Server/Dtos/EndTurnRequestDto.cs`:
```csharp
namespace RoguelikeCardGame.Server.Dtos;

public sealed record EndTurnRequestDto();
```

`src/Server/Dtos/PlayCardRequestDto.cs`:
```csharp
namespace RoguelikeCardGame.Server.Dtos;

public sealed record PlayCardRequestDto(
    int HandIndex,
    int? TargetEnemyIndex,
    int? TargetAllyIndex);
```

`src/Server/Dtos/UsePotionRequestDto.cs`:
```csharp
namespace RoguelikeCardGame.Server.Dtos;

public sealed record UsePotionRequestDto(
    int PotionIndex,
    int? TargetEnemyIndex,
    int? TargetAllyIndex);
```

`src/Server/Dtos/SetTargetRequestDto.cs`:
```csharp
namespace RoguelikeCardGame.Server.Dtos;

public sealed record SetTargetRequestDto(
    string Side,
    int SlotIndex);
```

- [ ] **Step 7: Build 確認**

```bash
dotnet build
```
Expected: 0 警告 0 エラー（DTO のみ追加なので既存テストへの影響なし）

- [ ] **Step 8: Commit + push**

```bash
git add src/Server/Dtos/
git commit -m "$(cat <<'EOF'
feat(server): add new battle DTOs (Phase 10.3-MVP Task 1)

spec §3: フル BattleStateDto + CombatActorDto + BattleCardInstanceDto +
BattleEventDto + BattleEventStepDto + BattleActionResponseDto + 5 リクエスト DTO
を追加。Task 3 で BattleStateDtoMapper、Task 4-10 で BattleController 各 endpoint
が利用。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 2: BattleSessionStore + DI 登録

**目的:** spec §4-1。in-memory `ConcurrentDictionary<accountId, BattleState>` で戦闘 state を保持。

**Files:**
- Create: `src/Server/Services/BattleSessionStore.cs`
- Modify: `src/Server/Program.cs` (DI 登録 1 行追加)

- [ ] **Step 1: `BattleSessionStore.cs` 作成**

`src/Server/Services/BattleSessionStore.cs`:
```csharp
using System.Collections.Concurrent;
using RoguelikeCardGame.Core.Battle.State;

namespace RoguelikeCardGame.Server.Services;

/// <summary>
/// 戦闘中の BattleState を accountId 単位でメモリ保持する。
/// Phase 10.3-MVP 暫定: save に乗らない (リロードで戦闘進行リセット)。
/// Phase 10.5 で本格保存への移行を検討。
/// </summary>
public sealed class BattleSessionStore
{
    private readonly ConcurrentDictionary<string, BattleState> _sessions = new();

    public bool TryGet(string accountId, out BattleState state)
        => _sessions.TryGetValue(accountId, out state!);

    public void Set(string accountId, BattleState state)
        => _sessions[accountId] = state;

    public void Remove(string accountId)
        => _sessions.TryRemove(accountId, out _);
}
```

- [ ] **Step 2: `Program.cs` で DI 登録**

`src/Server/Program.cs` で他の `services.AddSingleton<...>()` の隣に追加:
```csharp
services.AddSingleton<BattleSessionStore>();
```

`using RoguelikeCardGame.Server.Services;` の import が既存にあるはず（既存の `RunStartService` 等で）。なければ追加。

- [ ] **Step 3: Build 確認**

```bash
dotnet build
```
Expected: 0 警告 0 エラー

- [ ] **Step 4: Commit + push**

```bash
git add src/Server/Services/BattleSessionStore.cs src/Server/Program.cs
git commit -m "$(cat <<'EOF'
feat(server): add BattleSessionStore for in-memory battle state (Phase 10.3-MVP Task 2)

spec §4-1: ConcurrentDictionary<accountId, BattleState> で戦闘 state を
保持。Singleton DI 登録。Task 4 以降の BattleController endpoint で利用。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 3: BattleStateDtoMapper

**目的:** spec §4-2。`BattleState` → `BattleStateDto` 変換 + AttackPool/BlockPool の Display 値計算。

**Files:**
- Create: `src/Server/Services/BattleStateDtoMapper.cs`

- [ ] **Step 1: `BattleStateDtoMapper.cs` 作成**

`src/Server/Services/BattleStateDtoMapper.cs`:
```csharp
using System.Collections.Generic;
using System.Linq;
using RoguelikeCardGame.Core.Battle.Events;
using RoguelikeCardGame.Core.Battle.State;
using RoguelikeCardGame.Server.Dtos;

namespace RoguelikeCardGame.Server.Services;

internal static class BattleStateDtoMapper
{
    public static BattleStateDto ToDto(BattleState state) =>
        new(
            Turn: state.Turn,
            Phase: state.Phase.ToString(),
            Outcome: state.Outcome.ToString(),
            Allies: state.Allies.Select(ToActorDto).ToList(),
            Enemies: state.Enemies.Select(ToActorDto).ToList(),
            TargetAllyIndex: state.TargetAllyIndex,
            TargetEnemyIndex: state.TargetEnemyIndex,
            Energy: state.Energy,
            EnergyMax: state.EnergyMax,
            DrawPile: state.DrawPile.Select(ToCardDto).ToList(),
            Hand: state.Hand.Select(ToCardDto).ToList(),
            DiscardPile: state.DiscardPile.Select(ToCardDto).ToList(),
            ExhaustPile: state.ExhaustPile.Select(ToCardDto).ToList(),
            SummonHeld: state.SummonHeld.Select(ToCardDto).ToList(),
            PowerCards: state.PowerCards.Select(ToCardDto).ToList(),
            ComboCount: state.ComboCount,
            LastPlayedOrigCost: state.LastPlayedOrigCost,
            NextCardComboFreePass: state.NextCardComboFreePass,
            OwnedRelicIds: state.OwnedRelicIds.ToList(),
            Potions: state.Potions.ToList(),
            EncounterId: state.EncounterId);

    private static CombatActorDto ToActorDto(CombatActor a) =>
        new(
            InstanceId: a.InstanceId,
            DefinitionId: a.DefinitionId,
            Side: a.Side.ToString(),
            SlotIndex: a.SlotIndex,
            CurrentHp: a.CurrentHp,
            MaxHp: a.MaxHp,
            BlockDisplay: a.Block.Display(a.GetStatus("dexterity")),
            AttackSingleDisplay: a.AttackSingle.Display(a.GetStatus("strength"), a.GetStatus("weak")),
            AttackRandomDisplay: a.AttackRandom.Display(a.GetStatus("strength"), a.GetStatus("weak")),
            AttackAllDisplay: a.AttackAll.Display(a.GetStatus("strength"), a.GetStatus("weak")),
            Statuses: a.Statuses.ToDictionary(kv => kv.Key, kv => kv.Value),
            CurrentMoveId: a.CurrentMoveId,
            RemainingLifetimeTurns: a.RemainingLifetimeTurns,
            AssociatedSummonHeldInstanceId: a.AssociatedSummonHeldInstanceId);

    private static BattleCardInstanceDto ToCardDto(BattleCardInstance c) =>
        new(c.InstanceId, c.CardDefinitionId, c.IsUpgraded, c.CostOverride);

    public static BattleEventDto ToEventDto(BattleEvent ev) =>
        new(
            Kind: ev.Kind.ToString(),
            Order: ev.Order,
            CasterInstanceId: ev.CasterInstanceId,
            TargetInstanceId: ev.TargetInstanceId,
            Amount: ev.Amount,
            CardId: ev.CardId,
            Note: ev.Note);

    /// <summary>
    /// Phase 10.3-MVP 確定 (spec §3-5): SnapshotAfter は常に最終 state と同一。
    /// Client は CSS transition で HP/Block 数値を補間する。
    /// 将来 Phase 10.4 で本物の中間 state に進化させる場合、DTO 形式を維持して
    /// 中身だけ差し替え可能。
    /// </summary>
    public static BattleActionResponseDto ToActionResponse(
        BattleState finalState, IReadOnlyList<BattleEvent> events)
    {
        var stateDto = ToDto(finalState);
        var steps = events
            .Select(ev => new BattleEventStepDto(ToEventDto(ev), stateDto))
            .ToList();
        return new BattleActionResponseDto(stateDto, steps);
    }
}
```

注: `AttackPool.Display` / `BlockPool.Display` メソッドは 10.2.B 実装済み。`actor.GetStatus(string)` も既存。`BattlePool.Display` メソッドが見つからない場合は既存テストファイル (`AttackPoolTests.cs` 等) で正しい呼出シグネチャを確認。

- [ ] **Step 2: Build 確認**

```bash
dotnet build
```
Expected: 0 警告 0 エラー（Mapper のみ追加、まだ呼出元なし）

- [ ] **Step 3: Commit + push**

```bash
git add src/Server/Services/BattleStateDtoMapper.cs
git commit -m "$(cat <<'EOF'
feat(server): add BattleStateDtoMapper (Phase 10.3-MVP Task 3)

spec §4-2: BattleState → BattleStateDto 変換 + AttackPool/BlockPool の
Display 値計算 (10.2.B 実装済 Display メソッド利用)。
SnapshotAfter は常に最終 state と同一 (spec §3-5)。
Task 4 以降の BattleController endpoint で利用。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 4: BattleController.Start endpoint (+ F1 BestiaryTracker)

**目的:** spec §2-2 / §4-4 / §6-2 F1。新規戦闘開始 + 冪等性 (リロード対応) + 図鑑への敵記録。

**Files:**
- Create: `src/Server/Controllers/BattleController.cs` (Start endpoint のみ)
- Create: `tests/Server.Tests/Controllers/BattleControllerTests.cs` (Start 関連 4 テスト)
- Create: `tests/Server.Tests/Fixtures/BattleControllerFixtures.cs` (テスト共通 helper)

- [ ] **Step 1: Fixture file 作成**

`tests/Server.Tests/Fixtures/BattleControllerFixtures.cs`:
```csharp
using System;
using System.Collections.Immutable;
using System.Net.Http;
using System.Net.Http.Json;
using System.Threading.Tasks;
using Microsoft.AspNetCore.Mvc.Testing;
using Microsoft.Extensions.DependencyInjection;
using RoguelikeCardGame.Server.Services;

namespace RoguelikeCardGame.Server.Tests.Fixtures;

public static class BattleControllerFixtures
{
    public const string AccountHeader = "X-Account-Id";

    /// <summary>
    /// テスト用 WebApplicationFactory + アカウント作成 + 進行中ラン (戦闘進入直前) を準備。
    /// 既存 RunsControllerTests のパターンを流用 (アカウント作成 → /runs/new → /runs/current/move で
    /// 敵タイル進入)。
    /// </summary>
    public static async Task<(WebApplicationFactory<Program> factory, HttpClient client, string accountId)>
        SetupRunWithActiveBattleAsync(string accountId = "test-account")
    {
        var factory = new WebApplicationFactory<Program>();
        var client = factory.CreateClient();
        client.DefaultRequestHeaders.Add(AccountHeader, accountId);

        // 既存 fixture パターンに従ってアカウント + ラン作成 + 敵タイル進入
        // 詳細は tests/Server.Tests/Controllers/RunsControllerTests.cs 等を参照
        await client.PostAsJsonAsync("/api/v1/accounts", new { id = accountId });
        await client.PostAsync($"/api/v1/runs/new?force=true", null);
        // ... 敵タイル進入 (具体的な実装は既存テストフィクスチャ参照)

        return (factory, client, accountId);
    }

    public static void AssertNoSession(IServiceProvider services, string accountId)
    {
        var store = services.GetRequiredService<BattleSessionStore>();
        Assert.False(store.TryGet(accountId, out _));
    }
}
```

注: 既存 `tests/Server.Tests/` 配下に `Fixtures/` フォルダがなければ新規作成。既存の test fixture パターン (`RunsControllerTests` 等) を確認して `SetupRunWithActiveBattleAsync` の中身を完成させる。最低限 `RunState.ActiveBattle is not null` の状態を save に作れればよい。

- [ ] **Step 2: 失敗テスト 4 件を `BattleControllerTests.cs` に書く**

`tests/Server.Tests/Controllers/BattleControllerTests.cs`:
```csharp
using System.Net;
using System.Net.Http.Json;
using System.Threading.Tasks;
using RoguelikeCardGame.Server.Dtos;
using RoguelikeCardGame.Server.Tests.Fixtures;
using Xunit;

namespace RoguelikeCardGame.Server.Tests.Controllers;

public class BattleControllerTests
{
    [Fact]
    public async Task Start_when_no_active_run_returns_409()
    {
        var factory = new Microsoft.AspNetCore.Mvc.Testing.WebApplicationFactory<Program>();
        var client = factory.CreateClient();
        client.DefaultRequestHeaders.Add(BattleControllerFixtures.AccountHeader, "no-run-account");
        await client.PostAsJsonAsync("/api/v1/accounts", new { id = "no-run-account" });

        var resp = await client.PostAsync("/api/v1/runs/current/battle/start", null);

        Assert.Equal(HttpStatusCode.Conflict, resp.StatusCode);
    }

    [Fact]
    public async Task Start_when_no_active_battle_returns_409()
    {
        // ラン作成 (敵タイル進入なし) → ActiveBattle null
        var factory = new Microsoft.AspNetCore.Mvc.Testing.WebApplicationFactory<Program>();
        var client = factory.CreateClient();
        const string accountId = "no-battle-account";
        client.DefaultRequestHeaders.Add(BattleControllerFixtures.AccountHeader, accountId);
        await client.PostAsJsonAsync("/api/v1/accounts", new { id = accountId });
        await client.PostAsync("/api/v1/runs/new?force=true", null);

        var resp = await client.PostAsync("/api/v1/runs/current/battle/start", null);

        Assert.Equal(HttpStatusCode.Conflict, resp.StatusCode);
    }

    [Fact]
    public async Task Start_creates_session_and_returns_BattleStart_TurnStart_events()
    {
        var (factory, client, accountId) = await BattleControllerFixtures.SetupRunWithActiveBattleAsync();

        var resp = await client.PostAsync("/api/v1/runs/current/battle/start", null);

        Assert.Equal(HttpStatusCode.OK, resp.StatusCode);
        var body = await resp.Content.ReadFromJsonAsync<BattleActionResponseDto>();
        Assert.NotNull(body);
        Assert.Contains(body!.Steps, s => s.Event.Kind == "BattleStart");
        Assert.Contains(body!.Steps, s => s.Event.Kind == "TurnStart");
    }

    [Fact]
    public async Task Start_is_idempotent_returns_same_session()
    {
        var (factory, client, accountId) = await BattleControllerFixtures.SetupRunWithActiveBattleAsync();

        var first = await client.PostAsync("/api/v1/runs/current/battle/start", null);
        var second = await client.PostAsync("/api/v1/runs/current/battle/start", null);

        Assert.Equal(HttpStatusCode.OK, first.StatusCode);
        Assert.Equal(HttpStatusCode.OK, second.StatusCode);
        var firstBody = await first.Content.ReadFromJsonAsync<BattleActionResponseDto>();
        var secondBody = await second.Content.ReadFromJsonAsync<BattleActionResponseDto>();
        // 2 回目は events 空 (冪等性、既存セッションをそのまま返却)
        Assert.Empty(secondBody!.Steps);
        // 同じ Turn / Phase / Encounter
        Assert.Equal(firstBody!.State.Turn, secondBody.State.Turn);
        Assert.Equal(firstBody.State.EncounterId, secondBody.State.EncounterId);
    }
}
```

- [ ] **Step 3: テスト実行で失敗確認**

```bash
dotnet test --filter FullyQualifiedName~BattleControllerTests
```
Expected: コンパイルエラー (`BattleController` 未定義)

- [ ] **Step 4: `BattleController.cs` 実装 (Start endpoint)**

`src/Server/Controllers/BattleController.cs`:
```csharp
using System;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using Microsoft.AspNetCore.Mvc;
using RoguelikeCardGame.Core.Battle.Engine;
using RoguelikeCardGame.Core.Battle.Events;
using RoguelikeCardGame.Core.Battle.State;
using RoguelikeCardGame.Core.Bestiary;
using RoguelikeCardGame.Core.Data;
using RoguelikeCardGame.Core.Random;
using RoguelikeCardGame.Core.Run;
using RoguelikeCardGame.Server.Abstractions;
using RoguelikeCardGame.Server.Dtos;
using RoguelikeCardGame.Server.Services;

namespace RoguelikeCardGame.Server.Controllers;

[ApiController]
[Route("api/v1/runs/current/battle")]
public sealed class BattleController : ControllerBase
{
    public const string AccountHeader = "X-Account-Id";

    private readonly IAccountRepository _accounts;
    private readonly ISaveRepository _saves;
    private readonly DataCatalog _data;
    private readonly BattleSessionStore _sessions;
    private readonly RunStartService _runStart;
    private readonly IHistoryRepository _history;
    private readonly IBestiaryRepository _bestiary;

    public BattleController(
        IAccountRepository accounts,
        ISaveRepository saves,
        DataCatalog data,
        BattleSessionStore sessions,
        RunStartService runStart,
        IHistoryRepository history,
        IBestiaryRepository bestiary)
    {
        _accounts = accounts;
        _saves = saves;
        _data = data;
        _sessions = sessions;
        _runStart = runStart;
        _history = history;
        _bestiary = bestiary;
    }

    [HttpPost("start")]
    public async Task<IActionResult> Start(CancellationToken ct)
    {
        if (!TryGetAccountId(out var accountId, out var err)) return err!;
        if (!await _accounts.ExistsAsync(accountId, ct))
            return Problem(statusCode: StatusCodes.Status404NotFound,
                title: $"アカウントが見つかりません: {accountId}");

        var run = await _saves.TryLoadAsync(accountId, ct);
        if (run is null || run.Progress != RunProgress.InProgress)
            return Problem(statusCode: StatusCodes.Status409Conflict,
                title: "進行中のランがありません。");
        if (run.ActiveBattle is null)
            return Problem(statusCode: StatusCodes.Status409Conflict,
                title: "進行中の戦闘がありません。");

        // 冪等性: 既存セッションがあれば返す (リロード対応)
        if (_sessions.TryGet(accountId, out var existing))
        {
            return Ok(BattleStateDtoMapper.ToActionResponse(
                existing, Array.Empty<BattleEvent>()));
        }

        var rng = MakeBattleRng(run);
        var (state, events) = BattleEngine.Start(run, run.ActiveBattle.EncounterId, rng, _data);
        _sessions.Set(accountId, state);

        // F1 (spec §6-2): 図鑑に新敵を記録
        // 既存 BattlePlaceholder.Start でも実施されているが、新 BattleEngine.Start 経路でも維持
        if (_data.Encounters.TryGetValue(run.ActiveBattle.EncounterId, out var encounter))
        {
            var noted = BestiaryTracker.NoteEnemiesEncountered(run, encounter.EnemyIds);
            if (!ReferenceEquals(noted, run))
                await _saves.SaveAsync(accountId, noted, ct);
        }

        return Ok(BattleStateDtoMapper.ToActionResponse(state, events));
    }

    private bool TryGetAccountId(out string accountId, out IActionResult? err)
    {
        accountId = string.Empty;
        err = null;
        if (!Request.Headers.TryGetValue(AccountHeader, out var v) || string.IsNullOrEmpty(v))
        {
            err = Problem(statusCode: StatusCodes.Status400BadRequest,
                title: $"{AccountHeader} ヘッダが必要です。");
            return false;
        }
        accountId = v.ToString();
        return true;
    }

    private static IRng MakeBattleRng(RunState run) =>
        new SystemRng(unchecked((int)run.RngSeed ^ (int)run.PlaySeconds ^ 0xBA77LE));
}
```

- [ ] **Step 5: テスト実行で緑確認**

```bash
dotnet test --filter FullyQualifiedName~BattleControllerTests
```
Expected: 4 tests pass

- [ ] **Step 6: 全テスト緑 + Commit + push**

```bash
dotnet build
dotnet test
git add src/Server/Controllers/BattleController.cs tests/Server.Tests/Controllers/BattleControllerTests.cs tests/Server.Tests/Fixtures/BattleControllerFixtures.cs
git commit -m "$(cat <<'EOF'
feat(server): BattleController.Start endpoint + F1 bestiary (Phase 10.3-MVP Task 4)

spec §2-2 / §4-4 / §6-2 F1:
- POST /api/v1/runs/current/battle/start で BattleEngine.Start 呼出
- 冪等性: 既存セッションがあればそのまま返却 (リロード対応)
- 図鑑に新敵を記録 (BestiaryTracker.NoteEnemiesEncountered)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 5: BattleController.Get endpoint

**目的:** spec §2-3。リロード時の State 復元用。

**Files:**
- Modify: `src/Server/Controllers/BattleController.cs` (+Get method)
- Modify: `tests/Server.Tests/Controllers/BattleControllerTests.cs` (+2 tests)

- [ ] **Step 1: 失敗テスト 2 件追加**

`BattleControllerTests.cs` に追加:
```csharp
[Fact]
public async Task Get_when_session_exists_returns_BattleStateDto()
{
    var (_, client, _) = await BattleControllerFixtures.SetupRunWithActiveBattleAsync();
    await client.PostAsync("/api/v1/runs/current/battle/start", null);

    var resp = await client.GetAsync("/api/v1/runs/current/battle");

    Assert.Equal(HttpStatusCode.OK, resp.StatusCode);
    var body = await resp.Content.ReadFromJsonAsync<BattleStateDto>();
    Assert.NotNull(body);
    Assert.Equal("PlayerInput", body!.Phase);
}

[Fact]
public async Task Get_when_no_session_returns_404()
{
    var (_, client, _) = await BattleControllerFixtures.SetupRunWithActiveBattleAsync();
    // start を呼ばずに直接 GET

    var resp = await client.GetAsync("/api/v1/runs/current/battle");

    Assert.Equal(HttpStatusCode.NotFound, resp.StatusCode);
}
```

- [ ] **Step 2: テスト失敗確認**

```bash
dotnet test --filter FullyQualifiedName~BattleControllerTests.Get
```
Expected: コンパイルエラー or 404 でない

- [ ] **Step 3: `BattleController.Get` 実装**

`BattleController.cs` に追加:
```csharp
[HttpGet("")]
public async Task<IActionResult> Get(CancellationToken ct)
{
    if (!TryGetAccountId(out var accountId, out var err)) return err!;
    if (!await _accounts.ExistsAsync(accountId, ct))
        return Problem(statusCode: StatusCodes.Status404NotFound,
            title: $"アカウントが見つかりません: {accountId}");

    if (!_sessions.TryGet(accountId, out var state))
        return Problem(statusCode: StatusCodes.Status404NotFound,
            title: "戦闘セッションが存在しません。");

    return Ok(BattleStateDtoMapper.ToDto(state));
}
```

- [ ] **Step 4: テスト緑 + 全体 + commit + push**

```bash
dotnet test
git add src/Server/Controllers/BattleController.cs tests/Server.Tests/Controllers/BattleControllerTests.cs
git commit -m "$(cat <<'EOF'
feat(server): BattleController.Get endpoint (Phase 10.3-MVP Task 5)

spec §2-3: GET /api/v1/runs/current/battle でリロード時の state 復元。
session 無しで 404 (Client は POST /start で再開始)。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 6: BattleController.PlayCard endpoint

**目的:** spec §2-4 / §4-5。手札からカードプレイ。

**Files:**
- Modify: `src/Server/Controllers/BattleController.cs` (+PlayCard method)
- Modify: `tests/Server.Tests/Controllers/BattleControllerTests.cs` (+3 tests)

- [ ] **Step 1: 失敗テスト 3 件追加**

`BattleControllerTests.cs` に追加:
```csharp
[Fact]
public async Task PlayCard_with_valid_index_advances_state_and_returns_events()
{
    var (_, client, _) = await BattleControllerFixtures.SetupRunWithActiveBattleAsync();
    await client.PostAsync("/api/v1/runs/current/battle/start", null);

    var resp = await client.PostAsJsonAsync(
        "/api/v1/runs/current/battle/play-card",
        new PlayCardRequestDto(0, 0, 0));

    Assert.Equal(HttpStatusCode.OK, resp.StatusCode);
    var body = await resp.Content.ReadFromJsonAsync<BattleActionResponseDto>();
    Assert.NotNull(body);
    Assert.Contains(body!.Steps, s => s.Event.Kind == "PlayCard");
}

[Fact]
public async Task PlayCard_with_invalid_handIndex_returns_400()
{
    var (_, client, _) = await BattleControllerFixtures.SetupRunWithActiveBattleAsync();
    await client.PostAsync("/api/v1/runs/current/battle/start", null);

    var resp = await client.PostAsJsonAsync(
        "/api/v1/runs/current/battle/play-card",
        new PlayCardRequestDto(99, 0, 0));

    Assert.Equal(HttpStatusCode.BadRequest, resp.StatusCode);
}

[Fact]
public async Task PlayCard_when_no_session_returns_409()
{
    var (_, client, _) = await BattleControllerFixtures.SetupRunWithActiveBattleAsync();
    // start を呼ばずに play-card

    var resp = await client.PostAsJsonAsync(
        "/api/v1/runs/current/battle/play-card",
        new PlayCardRequestDto(0, 0, 0));

    Assert.Equal(HttpStatusCode.Conflict, resp.StatusCode);
}
```

- [ ] **Step 2: テスト失敗確認**

```bash
dotnet test --filter FullyQualifiedName~BattleControllerTests.PlayCard
```

- [ ] **Step 3: `PlayCard` 実装**

`BattleController.cs` に追加:
```csharp
[HttpPost("play-card")]
public async Task<IActionResult> PlayCard(
    [FromBody] PlayCardRequestDto body, CancellationToken ct)
{
    if (!TryGetAccountId(out var accountId, out var err)) return err!;
    if (body is null) return BadRequest();
    if (!await _accounts.ExistsAsync(accountId, ct))
        return Problem(statusCode: StatusCodes.Status404NotFound,
            title: $"アカウントが見つかりません: {accountId}");

    if (!_sessions.TryGet(accountId, out var state))
        return Problem(statusCode: StatusCodes.Status409Conflict,
            title: "戦闘セッションが存在しません。");

    var run = await _saves.TryLoadAsync(accountId, ct);
    if (run is null)
        return Problem(statusCode: StatusCodes.Status409Conflict,
            title: "進行中のランがありません。");

    var rng = MakeBattleRng(run);
    try
    {
        var (newState, events) = BattleEngine.PlayCard(
            state, body.HandIndex, body.TargetEnemyIndex, body.TargetAllyIndex, rng, _data);
        _sessions.Set(accountId, newState);
        return Ok(BattleStateDtoMapper.ToActionResponse(newState, events));
    }
    catch (InvalidOperationException ex)
    {
        return Problem(statusCode: StatusCodes.Status400BadRequest, title: ex.Message);
    }
}
```

- [ ] **Step 4: テスト緑 + commit + push**

```bash
dotnet test
git add src/Server/Controllers/BattleController.cs tests/Server.Tests/Controllers/BattleControllerTests.cs
git commit -m "$(cat <<'EOF'
feat(server): BattleController.PlayCard endpoint (Phase 10.3-MVP Task 6)

spec §2-4 / §4-5: BattleEngine.PlayCard 呼出、InvalidOperationException
(cost 不足 / handIndex 範囲外 / Phase 不正) を 400 に変換。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 7: BattleController.EndTurn endpoint

**目的:** spec §2-5。ターン終了 → PlayerAttacking → EnemyAttacking → 次 PlayerInput。

**Files:**
- Modify: `src/Server/Controllers/BattleController.cs` (+EndTurn method)
- Modify: `tests/Server.Tests/Controllers/BattleControllerTests.cs` (+2 tests)

- [ ] **Step 1: 失敗テスト 2 件追加**

```csharp
[Fact]
public async Task EndTurn_resolves_phase_transitions_and_returns_events()
{
    var (_, client, _) = await BattleControllerFixtures.SetupRunWithActiveBattleAsync();
    await client.PostAsync("/api/v1/runs/current/battle/start", null);

    var resp = await client.PostAsync("/api/v1/runs/current/battle/end-turn", null);

    Assert.Equal(HttpStatusCode.OK, resp.StatusCode);
    var body = await resp.Content.ReadFromJsonAsync<BattleActionResponseDto>();
    Assert.NotNull(body);
    Assert.Contains(body!.Steps, s => s.Event.Kind == "EndTurn");
    // ターン進行: 最終 state は PlayerInput または Resolved
    Assert.True(body.State.Phase == "PlayerInput" || body.State.Phase == "Resolved");
}

[Fact]
public async Task EndTurn_when_no_session_returns_409()
{
    var (_, client, _) = await BattleControllerFixtures.SetupRunWithActiveBattleAsync();
    var resp = await client.PostAsync("/api/v1/runs/current/battle/end-turn", null);
    Assert.Equal(HttpStatusCode.Conflict, resp.StatusCode);
}
```

- [ ] **Step 2: 失敗確認 → 実装 → 緑確認**

`BattleController.cs` に追加:
```csharp
[HttpPost("end-turn")]
public async Task<IActionResult> EndTurn(CancellationToken ct)
{
    if (!TryGetAccountId(out var accountId, out var err)) return err!;
    if (!await _accounts.ExistsAsync(accountId, ct))
        return Problem(statusCode: StatusCodes.Status404NotFound,
            title: $"アカウントが見つかりません: {accountId}");

    if (!_sessions.TryGet(accountId, out var state))
        return Problem(statusCode: StatusCodes.Status409Conflict,
            title: "戦闘セッションが存在しません。");

    var run = await _saves.TryLoadAsync(accountId, ct);
    if (run is null)
        return Problem(statusCode: StatusCodes.Status409Conflict,
            title: "進行中のランがありません。");

    var rng = MakeBattleRng(run);
    try
    {
        var (newState, events) = BattleEngine.EndTurn(state, rng, _data);
        _sessions.Set(accountId, newState);
        return Ok(BattleStateDtoMapper.ToActionResponse(newState, events));
    }
    catch (InvalidOperationException ex)
    {
        return Problem(statusCode: StatusCodes.Status400BadRequest, title: ex.Message);
    }
}
```

- [ ] **Step 3: テスト緑 + commit + push**

```bash
dotnet test
git add src/Server/Controllers/BattleController.cs tests/Server.Tests/Controllers/BattleControllerTests.cs
git commit -m "$(cat <<'EOF'
feat(server): BattleController.EndTurn endpoint (Phase 10.3-MVP Task 7)

spec §2-5: BattleEngine.EndTurn 呼出、Phase 進行 + events 返却。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 8: BattleController.UsePotion endpoint

**目的:** spec §2-6。ポーション使用。

**Files:**
- Modify: `src/Server/Controllers/BattleController.cs` (+UsePotion method)
- Modify: `tests/Server.Tests/Controllers/BattleControllerTests.cs` (+2 tests)

- [ ] **Step 1: 失敗テスト 2 件追加**

```csharp
[Fact]
public async Task UsePotion_with_valid_slot_consumes_potion()
{
    // ポーション保持の RunState を fixture で用意 (BattleControllerFixtures に拡張が必要)
    var (_, client, _) = await BattleControllerFixtures.SetupRunWithPotionAsync(potionId: "fire_potion");
    await client.PostAsync("/api/v1/runs/current/battle/start", null);

    var resp = await client.PostAsJsonAsync(
        "/api/v1/runs/current/battle/use-potion",
        new UsePotionRequestDto(0, 0, null));

    Assert.Equal(HttpStatusCode.OK, resp.StatusCode);
    var body = await resp.Content.ReadFromJsonAsync<BattleActionResponseDto>();
    Assert.NotNull(body);
    Assert.Contains(body!.Steps, s => s.Event.Kind == "UsePotion");
    Assert.Equal("", body.State.Potions[0]); // slot 消費
}

[Fact]
public async Task UsePotion_with_empty_slot_returns_400()
{
    var (_, client, _) = await BattleControllerFixtures.SetupRunWithActiveBattleAsync();
    await client.PostAsync("/api/v1/runs/current/battle/start", null);

    var resp = await client.PostAsJsonAsync(
        "/api/v1/runs/current/battle/use-potion",
        new UsePotionRequestDto(0, null, null));

    Assert.Equal(HttpStatusCode.BadRequest, resp.StatusCode);
}
```

注: `SetupRunWithPotionAsync` を `BattleControllerFixtures.cs` に追加する必要あり（既存 `SetupRunWithActiveBattleAsync` のラッパで `RunState.Potions[0]` に指定 ID をセット）。

- [ ] **Step 2: Fixture 拡張 → テスト失敗確認 → 実装 → 緑確認**

`BattleController.cs` に追加:
```csharp
[HttpPost("use-potion")]
public async Task<IActionResult> UsePotion(
    [FromBody] UsePotionRequestDto body, CancellationToken ct)
{
    if (!TryGetAccountId(out var accountId, out var err)) return err!;
    if (body is null) return BadRequest();
    if (!await _accounts.ExistsAsync(accountId, ct))
        return Problem(statusCode: StatusCodes.Status404NotFound,
            title: $"アカウントが見つかりません: {accountId}");

    if (!_sessions.TryGet(accountId, out var state))
        return Problem(statusCode: StatusCodes.Status409Conflict,
            title: "戦闘セッションが存在しません。");

    var run = await _saves.TryLoadAsync(accountId, ct);
    if (run is null)
        return Problem(statusCode: StatusCodes.Status409Conflict,
            title: "進行中のランがありません。");

    var rng = MakeBattleRng(run);
    try
    {
        var (newState, events) = BattleEngine.UsePotion(
            state, body.PotionIndex, body.TargetEnemyIndex, body.TargetAllyIndex, rng, _data);
        _sessions.Set(accountId, newState);
        return Ok(BattleStateDtoMapper.ToActionResponse(newState, events));
    }
    catch (InvalidOperationException ex)
    {
        return Problem(statusCode: StatusCodes.Status400BadRequest, title: ex.Message);
    }
}
```

- [ ] **Step 3: テスト緑 + commit + push**

```bash
dotnet test
git add src/Server/Controllers/BattleController.cs tests/Server.Tests/Controllers/BattleControllerTests.cs tests/Server.Tests/Fixtures/BattleControllerFixtures.cs
git commit -m "$(cat <<'EOF'
feat(server): BattleController.UsePotion endpoint (Phase 10.3-MVP Task 8)

spec §2-6: BattleEngine.UsePotion 呼出、空 slot で 400、消費後 Potions[i]="".

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 9: BattleController.SetTarget endpoint

**目的:** spec §2-7。ターゲット切替（events 無し、state のみ返却）。

**Files:**
- Modify: `src/Server/Controllers/BattleController.cs` (+SetTarget method)
- Modify: `tests/Server.Tests/Controllers/BattleControllerTests.cs` (+1 test)

- [ ] **Step 1: 失敗テスト追加**

```csharp
[Fact]
public async Task SetTarget_updates_target_indices_without_events()
{
    var (_, client, _) = await BattleControllerFixtures.SetupRunWithActiveBattleAsync();
    await client.PostAsync("/api/v1/runs/current/battle/start", null);

    var resp = await client.PostAsJsonAsync(
        "/api/v1/runs/current/battle/set-target",
        new SetTargetRequestDto("Enemy", 0));

    Assert.Equal(HttpStatusCode.OK, resp.StatusCode);
    var body = await resp.Content.ReadFromJsonAsync<BattleStateDto>();
    Assert.NotNull(body);
    Assert.Equal(0, body!.TargetEnemyIndex);
}
```

- [ ] **Step 2: 実装**

`BattleController.cs` に追加:
```csharp
[HttpPost("set-target")]
public async Task<IActionResult> SetTarget(
    [FromBody] SetTargetRequestDto body, CancellationToken ct)
{
    if (!TryGetAccountId(out var accountId, out var err)) return err!;
    if (body is null) return BadRequest();
    if (!await _accounts.ExistsAsync(accountId, ct))
        return Problem(statusCode: StatusCodes.Status404NotFound,
            title: $"アカウントが見つかりません: {accountId}");

    if (!_sessions.TryGet(accountId, out var state))
        return Problem(statusCode: StatusCodes.Status409Conflict,
            title: "戦闘セッションが存在しません。");

    if (!Enum.TryParse<ActorSide>(body.Side, out var side))
        return Problem(statusCode: StatusCodes.Status400BadRequest,
            title: $"不正な Side: {body.Side}");

    try
    {
        var newState = BattleEngine.SetTarget(state, side, body.SlotIndex);
        _sessions.Set(accountId, newState);
        return Ok(BattleStateDtoMapper.ToDto(newState));
    }
    catch (InvalidOperationException ex)
    {
        return Problem(statusCode: StatusCodes.Status400BadRequest, title: ex.Message);
    }
}
```

注: `BattleEngine.SetTarget` は 10.2.C で実装済。シグネチャを確認 (`SetTarget(state, ActorSide side, int slotIndex)`)。

- [ ] **Step 3: テスト緑 + commit + push**

```bash
dotnet test
git add src/Server/Controllers/BattleController.cs tests/Server.Tests/Controllers/BattleControllerTests.cs
git commit -m "$(cat <<'EOF'
feat(server): BattleController.SetTarget endpoint (Phase 10.3-MVP Task 9)

spec §2-7: BattleEngine.SetTarget 呼出、events 無しで BattleStateDto のみ返却。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 10: BattleController.Finalize endpoint (Victory/Defeat 分岐)

**目的:** spec §2-8 / §4-6。戦闘終了確定後の reward / GameOver 遷移。

**Files:**
- Modify: `src/Server/Controllers/BattleController.cs` (+Finalize, HandleVictoryAsync, HandleDefeatAsync methods)
- Modify: `tests/Server.Tests/Controllers/BattleControllerTests.cs` (+5 tests)

- [ ] **Step 1: 失敗テスト 5 件追加**

```csharp
[Fact]
public async Task Finalize_when_battle_not_resolved_returns_409()
{
    var (_, client, _) = await BattleControllerFixtures.SetupRunWithActiveBattleAsync();
    await client.PostAsync("/api/v1/runs/current/battle/start", null);
    // EndTurn を呼ばない → Phase=PlayerInput のまま

    var resp = await client.PostAsync("/api/v1/runs/current/battle/finalize", null);

    Assert.Equal(HttpStatusCode.Conflict, resp.StatusCode);
}

[Fact]
public async Task Finalize_Victory_creates_reward_and_clears_session()
{
    // 弱い敵で 1 ターンで勝利できる encounter を fixture で用意
    var (factory, client, accountId) = await BattleControllerFixtures.SetupRunWithEasyBattleAsync();
    await client.PostAsync("/api/v1/runs/current/battle/start", null);
    // 強カードで攻撃 → EndTurn → Victory
    // 詳細は具体的な encounter / カード定義に依存
    // ... (実装時に手動デバッグで Victory に至るシーケンスを確定)
    // (または BattleEngine 直接操作で session を Resolved 状態に書き換える test helper を用意)

    var resp = await client.PostAsync("/api/v1/runs/current/battle/finalize", null);

    Assert.Equal(HttpStatusCode.OK, resp.StatusCode);
    BattleControllerFixtures.AssertNoSession(factory.Services, accountId);
}

[Fact]
public async Task Finalize_Defeat_sets_Progress_GameOver_and_returns_RunResultDto()
{
    // hero HP=1 で敵に殴られて死ぬ encounter
    var (factory, client, accountId) = await BattleControllerFixtures.SetupRunWithLowHpAsync();
    // ... (Defeat に至るシーケンス)

    var resp = await client.PostAsync("/api/v1/runs/current/battle/finalize", null);

    Assert.Equal(HttpStatusCode.OK, resp.StatusCode);
    var result = await resp.Content.ReadFromJsonAsync<RunResultDto>();
    Assert.NotNull(result);
    Assert.Equal("GameOver", result!.Outcome);
}

[Fact]
public async Task Finalize_consumes_potions_in_RunState_Potions()
{
    var (factory, client, accountId) = await BattleControllerFixtures.SetupRunWithPotionAsync(potionId: "fire_potion");
    await client.PostAsync("/api/v1/runs/current/battle/start", null);
    await client.PostAsJsonAsync("/api/v1/runs/current/battle/use-potion",
        new UsePotionRequestDto(0, 0, null));
    // ... Victory に至る
    await client.PostAsync("/api/v1/runs/current/battle/finalize", null);

    // RunState を再 load して確認
    var snap = await client.GetFromJsonAsync<RunSnapshotDto>("/api/v1/runs/current");
    Assert.Equal("", snap!.Run.Potions[0]);
}

[Fact]
public async Task Finalize_when_no_session_returns_409()
{
    var (_, client, _) = await BattleControllerFixtures.SetupRunWithActiveBattleAsync();
    var resp = await client.PostAsync("/api/v1/runs/current/battle/finalize", null);
    Assert.Equal(HttpStatusCode.Conflict, resp.StatusCode);
}
```

注: 実装時に encounter / カード定義 / fixture の詳細を確定。Victory / Defeat 経路に至る最小シーケンスは既存テストパターン (`RunsControllerTests` の `PostBattleWin` 系) を参考。

- [ ] **Step 2: `Finalize` 実装**

`BattleController.cs` に追加（Victory/Defeat 分岐は既存 `RunsController.PostBattleWin` のロジックをコピー流用）:
```csharp
[HttpPost("finalize")]
public async Task<IActionResult> Finalize(CancellationToken ct)
{
    if (!TryGetAccountId(out var accountId, out var err)) return err!;
    if (!await _accounts.ExistsAsync(accountId, ct))
        return Problem(statusCode: StatusCodes.Status404NotFound,
            title: $"アカウントが見つかりません: {accountId}");

    if (!_sessions.TryGet(accountId, out var battleState))
        return Problem(statusCode: StatusCodes.Status409Conflict,
            title: "戦闘セッションが存在しません。");

    if (battleState.Phase != BattlePhase.Resolved)
        return Problem(statusCode: StatusCodes.Status409Conflict,
            title: "戦闘がまだ終了していません。");

    var run = await _saves.TryLoadAsync(accountId, ct);
    if (run is null)
        return Problem(statusCode: StatusCodes.Status409Conflict,
            title: "進行中のランがありません。");

    var (afterFinalize, summary) = BattleEngine.Finalize(battleState, run);
    afterFinalize = afterFinalize with { ActiveBattle = null };
    _sessions.Remove(accountId);

    if (summary.Outcome == RoguelikeCardGame.Core.Battle.State.BattleOutcome.Victory)
        return await HandleVictoryAsync(accountId, afterFinalize, run, ct);
    else
        return await HandleDefeatAsync(accountId, afterFinalize, ct);
}

private async Task<IActionResult> HandleVictoryAsync(
    string accountId, RunState afterFinalize, RunState beforeRun, CancellationToken ct)
{
    var enc = _data.Encounters[beforeRun.ActiveBattle!.EncounterId];
    bool isBoss = enc.Pool.Tier == EnemyTier.Boss;

    var rewardRng = new SystemRng(unchecked(
        (int)beforeRun.RngSeed ^ (int)beforeRun.PlaySeconds ^ 0x5EED));

    if (isBoss && afterFinalize.CurrentAct == RunConstants.MaxAct)
    {
        var finished = ActTransition.FinishRun(afterFinalize, RunProgress.Cleared);
        var clearMap = _runStart.RehydrateMap(finished.RngSeed, finished.CurrentAct);
        var rec = RunHistoryBuilder.From(accountId, finished, clearMap,
            finished.VisitedNodeIds.Length, RunProgress.Cleared);
        await _history.AppendAsync(accountId, rec, ct);
        await _bestiary.MergeAsync(accountId, rec, ct);
        await _saves.DeleteAsync(accountId, ct);
        return Ok(RunSnapshotDtoMapper.ToResultDto(rec));
    }

    RewardRngState newRng;
    RewardState reward;
    if (isBoss)
    {
        var r = BossRewardFlow.GenerateBossReward(afterFinalize, _data, rewardRng)
            ?? throw new InvalidOperationException(
                $"BossRewardFlow returned null for non-final act {afterFinalize.CurrentAct}.");
        reward = r;
        newRng = afterFinalize.RewardRngState;
    }
    else
    {
        var (r, nr) = RewardGenerator.Generate(
            new RewardContext.FromEnemy(enc.Pool),
            afterFinalize.RewardRngState,
            ImmutableArray.CreateRange(beforeRun.Relics),
            _data.RewardTables.TryGetValue($"act{beforeRun.CurrentAct}", out var tbl)
                ? tbl : _data.RewardTables["act1"],
            _data, rewardRng);
        reward = r; newRng = nr;
    }

    var updated = afterFinalize with
    {
        ActiveReward = reward,
        RewardRngState = newRng,
        SavedAtUtc = DateTimeOffset.UtcNow,
    };
    if (reward.CardChoices.Length > 0)
        updated = BestiaryTracker.NoteCardsSeen(updated, reward.CardChoices);
    await _saves.SaveAsync(accountId, updated, ct);
    var winMap = _runStart.RehydrateMap(updated.RngSeed, updated.CurrentAct);
    return Ok(RunSnapshotDtoMapper.From(updated, winMap, _data));
}

private async Task<IActionResult> HandleDefeatAsync(
    string accountId, RunState afterFinalize, CancellationToken ct)
{
    var defeatedMap = _runStart.RehydrateMap(afterFinalize.RngSeed, afterFinalize.CurrentAct);
    var rec = RunHistoryBuilder.From(accountId, afterFinalize, defeatedMap,
        afterFinalize.VisitedNodeIds.Length, RunProgress.GameOver);
    await _history.AppendAsync(accountId, rec, ct);
    await _bestiary.MergeAsync(accountId, rec, ct);
    await _saves.DeleteAsync(accountId, ct);
    return Ok(RunSnapshotDtoMapper.ToResultDto(rec));
}
```

`using` 追加: `System.Collections.Immutable`, `RoguelikeCardGame.Core.Battle.Definitions`, `RoguelikeCardGame.Core.History`, `RoguelikeCardGame.Core.Rewards`.

- [ ] **Step 3: テスト緑 + commit + push**

```bash
dotnet test
git add src/Server/Controllers/BattleController.cs tests/Server.Tests/Controllers/BattleControllerTests.cs tests/Server.Tests/Fixtures/BattleControllerFixtures.cs
git commit -m "$(cat <<'EOF'
feat(server): BattleController.Finalize endpoint with Victory/Defeat (Phase 10.3-MVP Task 10)

spec §2-8 / §4-6: BattleEngine.Finalize 呼出 + 既存 RunsController.PostBattleWin
の Victory/Defeat 経路をコピー流用。Boss + last act で Cleared、通常時は Reward
生成、Defeat で GameOver。session 削除 + ActiveBattle null セット。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 11: CatalogController に units / enemies endpoint 追加 (F3)

**目的:** spec §4-10 / §6-2 F3。Client の `useUnitCatalog` / `useEnemyCatalog` hook が必要。`GetPotions` は既存。

**Files:**
- Modify: `src/Server/Controllers/CatalogController.cs` (+GetUnits, +GetEnemies)
- Modify: `tests/Server.Tests/Controllers/` (CatalogController テストがあれば追加、なければ新規)

- [ ] **Step 1: `CatalogController.cs` に DTO + endpoint 追加**

`src/Server/Controllers/CatalogController.cs` に追加:
```csharp
public sealed record EnemyCatalogEntryDto(
    string Id,
    string Name,
    string ImageId,
    int Hp,
    string InitialMoveId);

public sealed record UnitCatalogEntryDto(
    string Id,
    string Name,
    string ImageId,
    int Hp,
    string InitialMoveId,
    int? LifetimeTurns);

[HttpGet("enemies")]
public IActionResult GetEnemies()
{
    var result = new Dictionary<string, EnemyCatalogEntryDto>(_data.Enemies.Count);
    foreach (var (id, def) in _data.Enemies)
    {
        result[id] = new EnemyCatalogEntryDto(
            def.Id, def.Name, def.ImageId, def.Hp, def.InitialMoveId);
    }
    return Ok(result);
}

[HttpGet("units")]
public IActionResult GetUnits()
{
    if (_data.Units is null) return Ok(new Dictionary<string, UnitCatalogEntryDto>());
    var result = new Dictionary<string, UnitCatalogEntryDto>(_data.Units.Count);
    foreach (var (id, def) in _data.Units)
    {
        result[id] = new UnitCatalogEntryDto(
            def.Id, def.Name, def.ImageId, def.Hp, def.InitialMoveId, def.LifetimeTurns);
    }
    return Ok(result);
}
```

- [ ] **Step 2: テスト追加 (任意、最小)**

既存 `CatalogControllerTests.cs` があれば 2 テスト追加（`GetEnemies_returns_dictionary` / `GetUnits_returns_dictionary`）。なければスキップ可（手動プレイで確認）。

- [ ] **Step 3: build + test 緑 + commit + push**

```bash
dotnet build
dotnet test
git add src/Server/Controllers/CatalogController.cs tests/Server.Tests/
git commit -m "$(cat <<'EOF'
feat(server): CatalogController に units / enemies endpoint 追加 (Phase 10.3-MVP Task 11, F3)

spec §4-10 / §6-2 F3: Client の useUnitCatalog / useEnemyCatalog hook が
表示情報取得に使用。GetPotions は既存維持。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 12: RunsController.PostAbandon で session cleanup (F2)

**目的:** spec §4-9 / §6-2 F2。戦闘中ラン放棄時に `BattleSessionStore` のメモリリーク防止。

**Files:**
- Modify: `src/Server/Controllers/RunsController.cs` (PostAbandon に `_sessions.Remove` 追加 + DI 注入)

- [ ] **Step 1: `RunsController` に `BattleSessionStore` 注入**

`src/Server/Controllers/RunsController.cs` のコンストラクタに追加:
```csharp
private readonly BattleSessionStore _sessions;

public RunsController(
    IAccountRepository accounts, ISaveRepository saves, RunStartService runStart,
    DataCatalog data, IHistoryRepository history, IBestiaryRepository bestiary,
    BattleSessionStore sessions)   // ← 追加
{
    _accounts = accounts; _saves = saves; _runStart = runStart;
    _data = data; _history = history; _bestiary = bestiary;
    _sessions = sessions;   // ← 追加
}
```

- [ ] **Step 2: `PostAbandon` で `_saves.DeleteAsync` 前に session 削除**

`src/Server/Controllers/RunsController.cs:346` 付近の `PostAbandon` 内、`await _saves.DeleteAsync(accountId, ct);` の **直前** に追加:
```csharp
_sessions.Remove(accountId);   // F2: 戦闘中放棄時の session cleanup
await _saves.DeleteAsync(accountId, ct);
```

- [ ] **Step 3: 既存テスト緑確認**

```bash
dotnet test --filter FullyQualifiedName~RunsControllerTests
```
Expected: 既存テスト全 pass（session 削除はテストされていなくても、追加が無害）

- [ ] **Step 4: Commit + push**

```bash
git add src/Server/Controllers/RunsController.cs
git commit -m "$(cat <<'EOF'
feat(server): RunsController.PostAbandon で session cleanup (Phase 10.3-MVP Task 12, F2)

spec §4-9 / §6-2 F2: 戦闘中ラン放棄時に BattleSessionStore.Remove を呼ぶ。
メモリリーク防止 (BattleSession が残り続ける問題への対応)。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 13: Client API types + battle.ts + catalog hooks

**目的:** spec §5-1 / §5-7。Server の新 endpoint / DTO に Client 側の型と関数を追加。

**Files:**
- Modify: `src/Client/src/api/types.ts` (新 DTO 型追加)
- Modify: `src/Client/src/api/battle.ts` (新 7 関数追加、既存 winBattle 維持)
- Modify: `src/Client/src/api/catalog.ts` (units / enemies の fetcher 追加)
- Create: `src/Client/src/hooks/useUnitCatalog.ts`
- Create: `src/Client/src/hooks/useEnemyCatalog.ts`

注: `usePotionCatalog` も spec §5-7 で言及されているが、`CatalogController.GetPotions` は既存なので、既存 hook があれば確認、なければ新規作成。

- [ ] **Step 1: TypeScript 型追加 (`src/Client/src/api/types.ts`)**

ファイル末尾に追加:
```typescript
export type BattlePhase = 'PlayerInput' | 'PlayerAttacking' | 'EnemyAttacking' | 'Resolved'
export type BattleOutcomeKind = 'Pending' | 'Victory' | 'Defeat'
export type ActorSide = 'Ally' | 'Enemy'
export type BattleEventKind =
  | 'BattleStart' | 'TurnStart' | 'PlayCard'
  | 'AttackFire' | 'DealDamage' | 'GainBlock'
  | 'ActorDeath' | 'EndTurn' | 'BattleEnd'
  | 'ApplyStatus' | 'RemoveStatus' | 'PoisonTick'
  | 'Heal' | 'Draw' | 'Discard' | 'Upgrade' | 'Exhaust'
  | 'GainEnergy' | 'Summon' | 'UsePotion'

export type CombatActorDto = {
  instanceId: string
  definitionId: string
  side: ActorSide
  slotIndex: number
  currentHp: number
  maxHp: number
  blockDisplay: number
  attackSingleDisplay: number
  attackRandomDisplay: number
  attackAllDisplay: number
  statuses: Record<string, number>
  currentMoveId: string | null
  remainingLifetimeTurns: number | null
  associatedSummonHeldInstanceId: string | null
}

export type BattleCardInstanceDto = {
  instanceId: string
  cardDefinitionId: string
  isUpgraded: boolean
  costOverride: number | null
}

export type BattleEventDto = {
  kind: BattleEventKind
  order: number
  casterInstanceId: string | null
  targetInstanceId: string | null
  amount: number | null
  cardId: string | null
  note: string | null
}

export type BattleStateDto = {
  turn: number
  phase: BattlePhase
  outcome: BattleOutcomeKind
  allies: CombatActorDto[]
  enemies: CombatActorDto[]
  targetAllyIndex: number | null
  targetEnemyIndex: number | null
  energy: number
  energyMax: number
  drawPile: BattleCardInstanceDto[]
  hand: BattleCardInstanceDto[]
  discardPile: BattleCardInstanceDto[]
  exhaustPile: BattleCardInstanceDto[]
  summonHeld: BattleCardInstanceDto[]
  powerCards: BattleCardInstanceDto[]
  comboCount: number
  lastPlayedOrigCost: number | null
  nextCardComboFreePass: boolean
  ownedRelicIds: string[]
  potions: string[]
  encounterId: string
}

export type BattleEventStepDto = {
  event: BattleEventDto
  snapshotAfter: BattleStateDto
}

export type BattleActionResponseDto = {
  state: BattleStateDto
  steps: BattleEventStepDto[]
}

export type EnemyCatalogEntryDto = {
  id: string
  name: string
  imageId: string
  hp: number
  initialMoveId: string
}

export type UnitCatalogEntryDto = {
  id: string
  name: string
  imageId: string
  hp: number
  initialMoveId: string
  lifetimeTurns: number | null
}
```

注: 既存 `BattleStateDto` は Task 0 で `BattlePlaceholderStateDto` にリネーム済み。新 `BattleStateDto` は Task 0 で書き換えた `RunStateDto.activeBattle` 型 (`BattlePlaceholderStateDto | null`) とは別物として共存。

- [ ] **Step 2: `src/Client/src/api/battle.ts` に 7 関数追加**

ファイル末尾に追加（既存 `winBattle` は維持）:
```typescript
import { ApiError } from './client'
import type {
  BattleActionResponseDto,
  BattleStateDto,
  RunResultDto,
  RunSnapshotDto,
} from './types'

export async function startBattle(accountId: string): Promise<BattleActionResponseDto> {
  return await apiRequest<BattleActionResponseDto>('POST', '/runs/current/battle/start', {
    accountId,
    body: {},
  })
}

export async function getBattle(accountId: string): Promise<BattleStateDto | null> {
  try {
    return await apiRequest<BattleStateDto>('GET', '/runs/current/battle', { accountId })
  } catch (e) {
    if (e instanceof ApiError && e.status === 404) return null
    throw e
  }
}

export async function playCard(
  accountId: string,
  body: { handIndex: number; targetEnemyIndex?: number; targetAllyIndex?: number },
): Promise<BattleActionResponseDto> {
  return await apiRequest<BattleActionResponseDto>('POST', '/runs/current/battle/play-card', {
    accountId,
    body,
  })
}

export async function endTurn(accountId: string): Promise<BattleActionResponseDto> {
  return await apiRequest<BattleActionResponseDto>('POST', '/runs/current/battle/end-turn', {
    accountId,
    body: {},
  })
}

export async function usePotion(
  accountId: string,
  body: { potionIndex: number; targetEnemyIndex?: number; targetAllyIndex?: number },
): Promise<BattleActionResponseDto> {
  return await apiRequest<BattleActionResponseDto>('POST', '/runs/current/battle/use-potion', {
    accountId,
    body,
  })
}

export async function setBattleTarget(
  accountId: string,
  body: { side: 'Ally' | 'Enemy'; slotIndex: number },
): Promise<BattleStateDto> {
  return await apiRequest<BattleStateDto>('POST', '/runs/current/battle/set-target', {
    accountId,
    body,
  })
}

export async function finalizeBattle(accountId: string): Promise<RunSnapshotDto | RunResultDto> {
  return await apiRequest<RunSnapshotDto | RunResultDto>('POST', '/runs/current/battle/finalize', {
    accountId,
    body: {},
  })
}
```

- [ ] **Step 3: `src/Client/src/api/catalog.ts` に enemies / units fetcher 追加**

既存パターンに従って追加:
```typescript
import type { EnemyCatalogEntryDto, UnitCatalogEntryDto } from './types'

export async function fetchEnemyCatalog(): Promise<Record<string, EnemyCatalogEntryDto>> {
  return await apiRequest<Record<string, EnemyCatalogEntryDto>>('GET', '/catalog/enemies', {})
}

export async function fetchUnitCatalog(): Promise<Record<string, UnitCatalogEntryDto>> {
  return await apiRequest<Record<string, UnitCatalogEntryDto>>('GET', '/catalog/units', {})
}
```

- [ ] **Step 4: hook 作成**

`src/Client/src/hooks/useEnemyCatalog.ts`:
```typescript
import { useEffect, useState } from 'react'
import { fetchEnemyCatalog } from '../api/catalog'
import type { EnemyCatalogEntryDto } from '../api/types'

export function useEnemyCatalog() {
  const [data, setData] = useState<Record<string, EnemyCatalogEntryDto>>({})
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    let cancelled = false
    fetchEnemyCatalog()
      .then((d) => { if (!cancelled) { setData(d); setLoading(false) } })
      .catch(() => { if (!cancelled) setLoading(false) })
    return () => { cancelled = true }
  }, [])

  return { data, loading, byId: (id: string) => data[id] }
}
```

`src/Client/src/hooks/useUnitCatalog.ts`: 同パターン (`UnitCatalogEntryDto` を使用)。

- [ ] **Step 5: build + test 緑確認**

```bash
cd src/Client
npm run build
cd ../..
```
Expected: 0 errors

- [ ] **Step 6: Commit + push**

```bash
git add src/Client/src/api/types.ts src/Client/src/api/battle.ts src/Client/src/api/catalog.ts src/Client/src/hooks/useEnemyCatalog.ts src/Client/src/hooks/useUnitCatalog.ts
git commit -m "$(cat <<'EOF'
feat(client): battle API types + 7 functions + catalog hooks (Phase 10.3-MVP Task 13)

spec §5-1 / §5-7: Server の新 endpoint / DTO に対応する TypeScript 型と関数。
useEnemyCatalog / useUnitCatalog hook を新規追加 (BattleScreen の表示情報用)。
既存 winBattle 関数 (placeholder 用) は維持、Phase 10.5 で削除予定。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 14: BattleScreen.tsx を本番 API 接続化

**目的:** spec §5-2。既存 demo 用 `BattleScreen.tsx` (battle-v10.html ポート) を API 接続版に書き換え + dtoAdapter + animation queue + onClick wiring。

**Files:**
- Modify: `src/Client/src/screens/BattleScreen.tsx` (大改修)
- Create: `src/Client/src/screens/battleScreen/dtoAdapter.ts` (DTO + catalog → 既存中間型変換)

**Strategy**: 既存 hardcoded `DEFAULT_*` データを完全削除、Props を `{ accountId, onBattleResolved }` に置き換え、API 接続 + animation queue を追加、`HandCard` / `Slot` / `Potion` / `<button className="end-turn">` に `onClick` wiring を追加。

このタスクは大改修だが既存ファイルの置き換えが中心。実装中はステップを細かく分けず、最終的に `dotnet test` + `npm run build` + 手動プレイで挙動確認。

- [ ] **Step 1: `src/Client/src/screens/battleScreen/dtoAdapter.ts` 作成**

ヘルパー関数を集約:

```typescript
import type {
  BattleStateDto,
  BattleCardInstanceDto,
  CombatActorDto,
  EnemyCatalogEntryDto,
  UnitCatalogEntryDto,
} from '../../api/types'

// 既存 BattleScreen.tsx の中間型 (RelicDemo / CharacterDemo / HandCardDemo / BuffDemo) を再利用
// それらを export して dtoAdapter からも参照できるようにする (BattleScreen.tsx で型 export)
import type {
  RelicDemo, CharacterDemo, HandCardDemo, BuffDemo, IntentDemo, HpLv,
} from '../BattleScreen'

type CardCatalog = { byId: (id: string) => any }    // 既存 useCardCatalog の型に合わせて調整
type RelicCatalog = { byId: (id: string) => any }
type PotionCatalog = { byId: (id: string) => any }
type EnemyCatalog = { byId: (id: string) => EnemyCatalogEntryDto | undefined }
type UnitCatalog = { byId: (id: string) => UnitCatalogEntryDto | undefined }

const HERO_FALLBACK = {
  name: '主人公',
  imageId: '☗',
  description: 'プレイヤー本体。HP 0 で敗北。',
}

export function hpLevel(cur: number, max: number): HpLv {
  const pct = max > 0 ? cur / max : 0
  if (pct <= 0) return '0'
  if (pct < 0.34) return '1'
  if (pct < 0.67) return '2'
  return '3'
}

export function toRelicDemo(relicId: string, catalog: RelicCatalog): RelicDemo {
  const def = catalog.byId(relicId)
  return {
    icon: def?.icon ?? '?',
    rarity: (def?.rarity ?? 'c') as RelicDemo['rarity'],
    name: def?.name ?? relicId,
    desc: def?.description ?? '',
  }
}

export function toCharacterDemo(
  actor: CombatActorDto,
  catalogs: { enemies: EnemyCatalog; units: UnitCatalog },
): CharacterDemo {
  const isHero = actor.definitionId === 'hero'
  const def = isHero
    ? HERO_FALLBACK
    : actor.side === 'Enemy'
      ? catalogs.enemies.byId(actor.definitionId)
      : catalogs.units.byId(actor.definitionId)

  const spriteKind: CharacterDemo['spriteKind'] = isHero
    ? 'hero'
    : actor.side === 'Ally' ? 'ally' : 'enemy'

  return {
    occupied: true,
    name: def?.name ?? actor.definitionId,
    desc: (def as any)?.description ?? '',
    sprite: (def as any)?.imageId ?? '?',
    spriteKind,
    hpCur: actor.currentHp,
    hpMax: actor.maxHp,
    hpLv: hpLevel(actor.currentHp, actor.maxHp),
    intent: undefined,    // MVP では intent 表示は最小、Phase 10.4 で詳細化
    buffs: toBuffs(actor),
  }
}

export function toBuffs(actor: CombatActorDto): BuffDemo[] {
  const buffs: BuffDemo[] = []
  if (actor.blockDisplay > 0) {
    buffs.push({
      kind: 'block', icon: '◆', num: actor.blockDisplay,
      name: 'ブロック', desc: 'このターン、ダメージを軽減する。',
    })
  }
  for (const [statusId, amount] of Object.entries(actor.statuses)) {
    if (amount <= 0) continue
    const meta = statusMeta(statusId)
    buffs.push({
      kind: meta.kind, icon: meta.icon, num: amount,
      name: meta.name, desc: meta.desc,
    })
  }
  return buffs
}

function statusMeta(id: string): { kind: BuffDemo['kind']; icon: string; name: string; desc: string } {
  switch (id) {
    case 'strength': return { kind: 'buff', icon: '✦', name: '力', desc: '与えるダメージが +N 増加する。' }
    case 'dexterity': return { kind: 'buff', icon: '◇', name: '敏捷', desc: 'ブロック値が +N 増加する。' }
    case 'vulnerable': return { kind: 'debuff', icon: '☠', name: '脆弱', desc: '受けるダメージが 1.5 倍。N ターン残存。' }
    case 'weak': return { kind: 'debuff', icon: '☄', name: '脱力', desc: '与えるダメージが 0.75 倍。N ターン残存。' }
    case 'poison': return { kind: 'debuff', icon: '☠', name: '毒', desc: 'ターン開始時に N ダメージ。スタック数が 1 減る。' }
    case 'omnistrike': return { kind: 'buff', icon: '✷', name: '全体攻撃', desc: '攻撃が全敵に当たる。N ターン残存。' }
    default: return { kind: 'buff', icon: '?', name: id, desc: '' }
  }
}

export function toHandCardDemo(
  card: BattleCardInstanceDto,
  catalog: CardCatalog,
  energy: number,
): HandCardDemo {
  const def = catalog.byId(card.cardDefinitionId)
  const baseCost = card.isUpgraded ? def?.upgradedCost ?? def?.cost : def?.cost
  const cost = card.costOverride ?? baseCost ?? null
  return {
    name: def?.name ?? card.cardDefinitionId,
    cost: cost ?? 'X',
    type: ((def?.cardType ?? 'attack') as string).toLowerCase() as HandCardDemo['type'],
    rarity: ((def?.rarity ?? 'c') as string).toLowerCase() as HandCardDemo['rarity'],
    playable: cost !== null && cost !== undefined && cost <= energy,
    desc: card.isUpgraded
      ? def?.upgradedDescription ?? def?.description ?? ''
      : def?.description ?? '',
  }
}
```

注: 既存 `useCardCatalog` / `useRelicCatalog` の戻り値型に合わせて `byId` の型を調整。実装時に既存 hook を確認。

- [ ] **Step 2: `BattleScreen.tsx` を全面書き換え**

既存 `src/Client/src/screens/BattleScreen.tsx` を以下に置換:

```typescript
import { useCallback, useEffect, useState } from 'react'
import type { CSSProperties } from 'react'
import { Card } from '../components/Card'
import { useTooltipTarget } from '../components/Tooltip'
import {
  startBattle, getBattle, playCard, endTurn, usePotion, setBattleTarget, finalizeBattle,
} from '../api/battle'
import type {
  BattleStateDto, BattleActionResponseDto, ActorSide, RunResultDto, RunSnapshotDto,
} from '../api/types'
import { useCardCatalog } from '../hooks/useCardCatalog'
import { useRelicCatalog } from '../hooks/useRelicCatalog'
import { useEnemyCatalog } from '../hooks/useEnemyCatalog'
import { useUnitCatalog } from '../hooks/useUnitCatalog'
import {
  toRelicDemo, toCharacterDemo, toHandCardDemo, hpLevel,
} from './battleScreen/dtoAdapter'
import './BattleScreen.css'

// 既存 BattleScreen.tsx の中間型を export (dtoAdapter から参照)
export type HpLv = '0' | '1' | '2' | '3'
export type RelicDemo = { /* ... 既存通り ... */ }
export type BuffDemo = { /* ... */ }
export type IntentDemo = { /* ... */ }
export type CharacterDemo = { /* ... */ }
export type HandCardDemo = { /* ... */ }
// (上記の型定義は既存ファイルから流用)

type Props = {
  accountId: string
  onBattleResolved: (result: RunSnapshotDto | RunResultDto) => void
}

const STEP_DELAY_MS = 220

function sleep(ms: number): Promise<void> {
  return new Promise((resolve) => setTimeout(resolve, ms))
}

export function BattleScreen({ accountId, onBattleResolved }: Props) {
  const [state, setState] = useState<BattleStateDto | null>(null)
  const [animating, setAnimating] = useState(false)
  const [busy, setBusy] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const cardCatalog = useCardCatalog()
  const relicCatalog = useRelicCatalog()
  const enemyCatalog = useEnemyCatalog()
  const unitCatalog = useUnitCatalog()

  // mount: GET /battle → なければ POST /start
  useEffect(() => {
    let cancelled = false
    async function init() {
      try {
        const existing = await getBattle(accountId)
        if (cancelled) return
        if (existing) {
          setState(existing)
        } else {
          const resp = await startBattle(accountId)
          if (cancelled) return
          await playSteps(resp)
        }
      } catch (e) {
        if (!cancelled) setError((e as Error).message)
      }
    }
    void init()
    return () => { cancelled = true }
  }, [accountId])

  async function playSteps(resp: BattleActionResponseDto) {
    setAnimating(true)
    for (const step of resp.steps) {
      setState(step.snapshotAfter)
      await sleep(STEP_DELAY_MS)
    }
    setState(resp.state)
    setAnimating(false)

    if (resp.state.outcome === 'Victory' || resp.state.outcome === 'Defeat') {
      await handleFinalize()
    }
  }

  async function withBusy<T>(fn: () => Promise<T>): Promise<T | null> {
    if (animating || busy) return null
    setBusy(true)
    setError(null)
    try {
      return await fn()
    } catch (e) {
      setError((e as Error).message)
      return null
    } finally {
      setBusy(false)
    }
  }

  async function handlePlayCard(handIndex: number) {
    const resp = await withBusy(() => playCard(accountId, { handIndex }))
    if (resp) await playSteps(resp)
  }

  async function handleEndTurn() {
    const resp = await withBusy(() => endTurn(accountId))
    if (resp) await playSteps(resp)
  }

  async function handleUsePotion(potionIndex: number) {
    const resp = await withBusy(() => usePotion(accountId, { potionIndex }))
    if (resp) await playSteps(resp)
  }

  async function handleSetTarget(side: ActorSide, slotIndex: number) {
    const newState = await withBusy(() => setBattleTarget(accountId, { side, slotIndex }))
    if (newState) setState(newState)
  }

  async function handleFinalize() {
    try {
      const result = await finalizeBattle(accountId)
      onBattleResolved(result)
    } catch (e) {
      setError((e as Error).message)
    }
  }

  if (state === null) {
    return <div className="battle__loading">戦闘準備中…</div>
  }

  // データソース変換
  const hero = state.allies.find(a => a.definitionId === 'hero')
  const heroHp = hero ? { cur: hero.currentHp, max: hero.maxHp, lv: hpLevel(hero.currentHp, hero.maxHp) } : { cur: 0, max: 0, lv: '0' as const }
  const relicDemos = state.ownedRelicIds.map(id => toRelicDemo(id, relicCatalog as any))
  const playerSlots = padSlots(
    state.allies.map(a => toCharacterDemo(a, { enemies: enemyCatalog as any, units: unitCatalog as any })),
    4)
  const enemySlots = padSlots(
    state.enemies.map(e => toCharacterDemo(e, { enemies: enemyCatalog as any, units: unitCatalog as any })),
    4)
  const handDemos = state.hand.map(c => toHandCardDemo(c, cardCatalog as any, state.energy))

  // ... 既存 BattleScreen.tsx の JSX 構造をベースに、データソースを demo データから上記変数に差し替え
  // onClick wiring を追加:
  //   - HandCard: onClick={() => handlePlayCard(i)}
  //   - Slot (enemy): onClick={() => handleSetTarget('Enemy', actor.slotIndex)}
  //   - Slot (ally): 同
  //   - <button className="end-turn">: onClick={handleEndTurn}
  //   - Potion: onClick={() => handleUsePotion(i)}
  // is-targeted CSS class: state.targetEnemyIndex / targetAllyIndex と一致する slot に付与

  return (
    <div className="battle">
      {/* ... 既存 layout 流用 ... */}
      {error && <div className="battle__error">{error}</div>}
    </div>
  )
}

function padSlots(chars: CharacterDemo[], n: number): CharacterDemo[] {
  const out = [...chars]
  while (out.length < n) {
    out.push({
      occupied: false, name: '', desc: '', sprite: '',
      spriteKind: 'enemy', hpCur: 0, hpMax: 0, hpLv: '0', buffs: [],
    })
  }
  return out.slice(0, n)
}
```

注: 上記は構造的な擬似コード。実装時には既存 `BattleScreen.tsx` の JSX 部分（HUD / Stage / Hand layout）を保ちつつ、データソースとイベントハンドラを差し替える。CSS は無変更。

具体的な書き換え方針:
- 既存の `DEFAULT_*` 定数を全削除
- 既存 `Props` を `{ accountId, onBattleResolved }` に置換
- `useState<BattleStateDto | null>` で API state 管理
- `useEffect` で初期 fetch
- 各 onClick handler を上記パターンで実装
- JSX 内の data source を hardcoded から DTO 由来データに変更

- [ ] **Step 3: build + 手動確認**

```bash
cd src/Client
npm run build
cd ../..
```
Expected: 0 errors（コンパイルが通れば次のタスクで実プレイ確認）

- [ ] **Step 4: Commit + push**

```bash
git add src/Client/src/screens/BattleScreen.tsx src/Client/src/screens/battleScreen/dtoAdapter.ts
git commit -m "$(cat <<'EOF'
feat(client): BattleScreen を本番 API 接続化 (Phase 10.3-MVP Task 14)

spec §5-2: 既存 demo 用 BattleScreen.tsx (battle-v10.html ポート) を API 接続版に
書き換え。dtoAdapter で DTO + catalog → 既存中間型 (RelicDemo / CharacterDemo /
HandCardDemo) 変換。events を 200ms ステップで順次再生 (CSS transition で数値補間)。
HandCard / Slot / Potion / EndTurn ボタンに onClick wiring。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 15: BattleOverlay 退役 + MapScreen 置換 + App.tsx 修正

**目的:** spec §5-3 / §5-4 / §5-6。`BattleOverlay.tsx` 削除、`MapScreen` で `BattleScreen` にスイッチ、`?demo=battle` URL 廃止。

**Files:**
- Delete: `src/Client/src/screens/BattleOverlay.tsx`
- Delete: `src/Client/src/screens/BattleOverlay.test.tsx`
- Modify: `src/Client/src/screens/MapScreen.tsx` (snapshot.battle ありで BattleScreen 表示)
- Modify: `src/Client/src/App.tsx` (?demo=battle ガード削除)

- [ ] **Step 1: `BattleOverlay.tsx` / test 削除**

```bash
git rm src/Client/src/screens/BattleOverlay.tsx
git rm src/Client/src/screens/BattleOverlay.test.tsx
```

- [ ] **Step 2: `MapScreen.tsx` の置換**

`src/Client/src/screens/MapScreen.tsx` 内の `<BattleOverlay>` 利用箇所を以下に書き換え:

```typescript
import { BattleScreen } from './BattleScreen'

// snapshot.battle ありの分岐内:
if (snapshot.run.activeBattle) {
  return (
    <BattleScreen
      accountId={accountId}
      onBattleResolved={(result) => {
        // RunResultDto は outcome field を持つ、RunSnapshotDto は run/map field を持つ
        if ('run' in result) {
          // RunSnapshotDto - reward 画面 or 通常マップ
          setSnapshot(result)
          // 親の onSnapshotUpdated 等があれば呼ぶ (既存パターンに従う)
        } else {
          // RunResultDto - ラン終了
          onRunFinished(result, snapshot)
        }
      }}
    />
  )
}
```

注: `MapScreen.tsx` の既存の battle 処理ロジック（`<BattleOverlay battle={...} onWin={...} />` の部分）を上記に置換。`BattleOverlay` の import を削除。

- [ ] **Step 3: `App.tsx` の `?demo=battle` ガード削除**

`src/Client/src/App.tsx:28-34` の以下を削除:
```typescript
if (typeof window !== 'undefined' && window.location.search.includes('demo=battle')) {
  return (
    <TooltipHost>
      <BattleScreen />
    </TooltipHost>
  )
}
```

`import { BattleScreen } from './screens/BattleScreen'` も削除（App では使わなくなる、MapScreen 内で使う）。

- [ ] **Step 4: build + test 確認**

```bash
cd src/Client
npm run build
npm test
cd ../..
```
Expected: 0 errors（`MapScreen.test.tsx` 等で BattleOverlay 参照が残っていないか確認）

- [ ] **Step 5: Commit + push**

```bash
git add src/Client/src/App.tsx src/Client/src/screens/MapScreen.tsx
git commit -m "$(cat <<'EOF'
refactor(client): retire BattleOverlay, switch MapScreen to BattleScreen (Phase 10.3-MVP Task 15)

spec §5-3 / §5-4 / §5-6:
- BattleOverlay.tsx / BattleOverlay.test.tsx 削除
- MapScreen が snapshot.run.activeBattle ありで BattleScreen をフルスクリーン表示
- App.tsx の ?demo=battle ガード削除 (BattleScreen は本番フローで動作)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 16: 親 spec 補記 + 手動プレイ確認 + tag

**目的:** spec §7-3 / §7-4。完了マーカー + 親 spec への補記反映 + 手動プレイ確認チェックリスト全項目完遂。

**Files:**
- Modify: `docs/superpowers/specs/2026-04-25-phase10-battle-system-design.md` (3 補記項目追加)

- [ ] **Step 1: 親 spec への補記事項反映**

`docs/superpowers/specs/2026-04-25-phase10-battle-system-design.md` の §10-3 / §3-3 / §9-7 該当章に以下 3 件を `> **Phase 10.3-MVP 補記**: ...` blockquote 形式で追記:

1. **§10-3**: Phase 10.3 を「10.3-MVP (REST + in-memory)」と「10.3 本格 (SignalR + 永続化)」に分割。MVP の API contract / Client 結線方針を補記
2. **§3-3 BattleStateDto**: フル DTO + AttackPool/BlockPool は Display 値計算済 + Catalog 引きを Client 責任とする方針を追記
3. **§9-7 BattleEventDto**: `BattleEventStepDto { event, snapshotAfter }` を導入、`SnapshotAfter` は最終 state と同一 (近似演出)、Client は CSS transition で補間

10.2.D / 10.2.E の補記スタイルを真似る（既存 `> **Phase 10.2.D 補記**:` 等を参照）。

- [ ] **Step 2: 全テスト最終確認**

```bash
dotnet build
dotnet test
cd src/Client && npm run build && cd ../..
```
Expected: 警告 0 / エラー 0、全テスト緑

- [ ] **Step 3: 手動プレイ確認 (spec §6-5 チェックリスト)**

```bash
dotnet run --project src/Server &
cd src/Client && npm run dev &
```

ブラウザで以下を順に確認:

- [ ] 新規ラン開始 → マップで敵タイル進入 → BattleScreen 表示
- [ ] 手札カードクリック → カード効果適用 + Energy 消費 + Discard へ移動
- [ ] エネミーをクリック → ターゲット切替（視覚ハイライト）
- [ ] End Turn → 攻撃発射 events 演出 → 敵行動 → 次ターン開始
- [ ] ポーションクリック → 効果発動 + slot 空に
- [ ] Victory: 全敵撃破 → finalize → Reward 画面に遷移
- [ ] Defeat: hero HP 0 → finalize → RunResult 画面 (GameOver)
- [ ] Boss + last act 撃破 → ラン Cleared
- [ ] レリック発動: OnTurnStart relic 持ちでターン開始時に効果発火
- [ ] ブラウザリロード: 戦闘中に F5 → 戦闘リセットして同 encounter で再開
- [ ] 既存 placeholder 経路 (`POST /battle/win`) が無傷で動作（curl 等で確認）
- [ ] 図鑑に新敵が記録されている（F1）
- [ ] 戦闘中にラン放棄 → session が削除されている（F2）

確認後、サーバ / Client dev server を停止。

- [ ] **Step 4: Commit + tag + push**

```bash
git add docs/superpowers/specs/2026-04-25-phase10-battle-system-design.md
git commit -m "$(cat <<'EOF'
docs(spec): amend Phase 10 spec for Phase 10.3-MVP decisions (Phase 10.3-MVP Task 16)

10.3-MVP spec §7-4 で予告した 3 項目を親 spec に補記:
- §10-3 Phase 10.3 を MVP / 本格に分割、API contract と Client 結線方針
- §3-3 BattleStateDto フル DTO + Display 値計算済 + Client catalog 引き責任
- §9-7 BattleEventStepDto.SnapshotAfter は最終 state と同一 (近似演出)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git tag phase10-3-mvp-complete
git push origin master phase10-3-mvp-complete
```

- [ ] **Step 5: 完了報告メモ**

10.3-MVP 完了に伴い、以下が達成された:
- `BattleEngine` (Phase 10.2 完成) が実プレイで動作
- REST 7 endpoint で戦闘進行
- battle-v10.html ベースの polished UI で実プレイ可能
- リロード時の戦闘 reset / 再開動作

次フェーズ:
- Phase 10.3 本格: SignalR `BattleHub` + 戦闘 state 永続化
- Phase 10.4: Client polish (event Kind 別演出、カードアニメーション、vitest 結合テスト)
- Phase 10.5: `BattlePlaceholder` 退役 + `RunState.ActiveBattle` 型切替 + save schema v8 + 戦闘外 UsePotion
- Phase 9: マルチプレイ協力戦闘 (後回し方針)
- Phase 11+: `OnPotionUse` レリック / 複数 summon / 召喚 actor 自身の attack 等

---

## Self-Review

実施日: 2026-04-27（plan 作成時）

### 1. Spec coverage チェック

| spec section | 該当 task |
|---|---|
| §1 アーキテクチャ概要 | 全 task で実装 |
| §2-1 エンドポイント一覧 | Task 4-10 |
| §2-2 Start | Task 4 |
| §2-3 Get | Task 5 |
| §2-4 PlayCard | Task 6 |
| §2-5 EndTurn | Task 7 |
| §2-6 UsePotion | Task 8 |
| §2-7 SetTarget | Task 9 |
| §2-8 Finalize | Task 10 |
| §2-9 リロード時 Client 起動シーケンス | Task 14 (BattleScreen.useEffect) |
| §3 全 DTO | Task 0 (rename) + Task 1 (新 DTO) |
| §4-1 BattleSessionStore | Task 2 |
| §4-2 BattleStateDtoMapper | Task 3 |
| §4-3 BattleController | Task 4-10 |
| §4-9 RunsController.Abandon session cleanup | Task 12 |
| §4-10 CatalogController 拡張 | Task 11 |
| §5-1 battle.ts 拡張 | Task 13 |
| §5-2 BattleScreen 大改修 | Task 14 |
| §5-3 App.tsx 修正 | Task 15 |
| §5-4 MapScreen 修正 | Task 15 |
| §5-6 BattleOverlay 削除 | Task 15 |
| §5-7 catalog hooks | Task 13 |
| §6-1 BattleControllerTests | Task 4-10 |
| §6-2 暗黙要件 F1 (BestiaryTracker) | Task 4 |
| §6-2 暗黙要件 F2 (session cleanup) | Task 12 |
| §6-2 暗黙要件 F3 (catalog endpoints) | Task 11 |
| §6-2 暗黙要件 F4 (run.Relics 渡し) | 既に 10.2.E で実装済、Task 4 で確認 |
| §6-5 手動プレイ確認 | Task 16 |
| §7-4 親 spec 補記 | Task 16 |
| §7-3 phase10-3-mvp-complete tag | Task 16 |

全 spec section にタスクが対応している。

### 2. Placeholder スキャン

- "TBD" / "TODO" / "implement later" を grep → なし（実装ガイダンスは全て具体的に記述）
- 各 step に実コード / 実コマンドが記載
- exception messages, file paths, method signatures すべて具体

### 3. Type consistency チェック

- `BattleStateDto` (Task 0 で rename → BattlePlaceholderStateDto、Task 1 で新規 BattleStateDto) — 一貫
- `CombatActorDto.Side: string` (Task 1) ↔ Client `ActorSide = 'Ally' | 'Enemy'` (Task 13) — 一貫
- `BattleEventDto.Kind: string` (Task 1) ↔ Client `BattleEventKind` literal union (Task 13) — 一貫
- `BattleSessionStore.TryGet/Set/Remove` (Task 2) ↔ 全 endpoint で利用 (Task 4-10) ↔ RunsController.Abandon (Task 12) — 一貫
- `BattleStateDtoMapper.ToDto/ToActionResponse/ToEventDto` (Task 3) ↔ 全 endpoint で利用 (Task 4-10) — 一貫
- `BattleEngine.{Start,PlayCard,EndTurn,UsePotion,SetTarget,Finalize}` 公開 API (Phase 10.2 完成) ↔ 各 endpoint で呼出 (Task 4-10) — 一貫
- Client 関数 `startBattle/getBattle/playCard/endTurn/usePotion/setBattleTarget/finalizeBattle` (Task 13) ↔ BattleScreen で利用 (Task 14) — 一貫

### 4. memory feedback ルール遵守チェック

- `BattleOutcome` の fully qualified: Task 10 Finalize で `RoguelikeCardGame.Core.Battle.State.BattleOutcome.Victory` 使用
- `state.Allies` への書き戻し: BattleController は BattleEngine 委譲のみで該当なし、BattleStateDtoMapper は read-only
- `BattleStateDtoMapper` の `actor.GetStatus(...)` は read-only

### 5. ビルド赤期間管理

- Task 0: rename は 1 commit でまとめる（Server + Client 同時更新で緑復帰）
- Task 1-3: 新規追加のみ、既存に影響なし
- Task 4-10: 各 endpoint は独立、TDD で各 task 内で緑復帰
- Task 11-12: 既存 controller 修正、最小変更で緑維持
- Task 13: Client API 追加のみ、既存に影響なし
- Task 14: BattleScreen 大改修、build 通すまで Task 14 内で完結
- Task 15: BattleOverlay 削除 + MapScreen / App.tsx 同 commit で整合性維持

すべて plan 通りに進めれば、各 task 完了時に緑復帰する。

問題なし。
